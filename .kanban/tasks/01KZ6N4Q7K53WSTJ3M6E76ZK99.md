---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01kzbj1apfzxqnnt977apfzew0
  text: |-
    Picked up. Research done — key findings before implementing:

    **Router surface confirmed present** in `.build/checkouts/FoundationModelsRouter` (b5bf2ba): `SessionMailbox.respond(elicitationId:_:) -> ElicitationAnswerDelivery`, `.complete(elicitationId:) -> ElicitationCompletionDelivery`, `ToolContext.elicit(_:) async throws -> ElicitationResponse`, plus `status() -> [RunStatus]`, `wait(completionToken:seconds:) -> WaitResult`, `cancel(completionToken:) -> CancelResult`, `park(tool:op:kind:completionToken:settling:canceler:)`, `terminalDetailTailLimit = 4_096`.

    **Seam facts that shape the design:**
    - `RunBinding.ambient` is read once at the top of `MultiTool.call(arguments:)`; the six new globals close over that same captured binding — no second capture path, no task-local inheritance across the JS seam.
    - `JSCInterpreter` prefixes every async-host-function rejection with the global's own name (`"<name>: <error>"`, JSCInterpreter `rejectWithMessage`), so the named error's `description` carries only the message half.
    - `OperationEventKind` has exactly three cases (`progress`, `completed`, `elicitation`) — there is no notification kind. `notify()` therefore posts a `.progress`-kind event through `ToolContext.post(_:)` and `progress()` posts through `ToolContext.progress(_:)`, exactly as the card and eventplan §"The capability contract" state. Router's `OperationEventKind` is the seam where a distinct notice kind would land.
    - `notify`/`progress` are synchronous void `HostFunction`s, so they cannot await. They enqueue onto a per-invocation FIFO delivery chain (the same `latestDelivery` shape `ElevatingTool`'s funnel uses) that delivers live and is awaited once after the run — "the bridge flushes them".
    - `ResultRenderer`'s default `returnValueCharacterLimit` is 4_000, *below* `terminalDetailTailLimit` (4_096), so the bounded-`wait()` test asserts on `.detail.length`, not the detail text.
    - Elicitation round trips are made deterministic (not timing-dependent) by `SessionMailbox.awaitAnswer`'s documented registration-happens-before-post ordering: a scripted sink answers from inside its own `post(_:)`.
  timestamp: 2026-08-06T12:51:10.159506+00:00
- actor: claude-code
  id: 01kzbk350hr4m8t16gy8c5azc6
  text: |-
    Implementation landed. Design notes for the next agent:

    **File placement.** The card says the factories go "in `MultiTool.swift`". They are `MultiTool.makeHelpDocsHostFunctions`-style static factories **on `MultiTool`** (`makeRunPlaneHostFunctions(binding:)`, `makeNoticeHostFunctions(outbox:)`), but they live in `Sources/FoundationModelsMultitool/MultiTool+SandboxGlobals.swift` rather than in `MultiTool.swift` itself. Reason: the six globals plus their marshaling and the named error are ~560 documented lines, and this repo already has the `Type+Feature.swift` convention (`Surface/APISurface+SearchableMetadata.swift`). `MultiTool.swift` keeps only the wiring in `call(arguments:)` and the preamble line. Substance is unchanged; if a reviewer wants them physically in `MultiTool.swift`, it is a file move.

    **`notify()`/`progress()` really return `undefined`, not `null`.** `InterpreterValue` — the seam every `HostFunction` result crosses — is JSON-shaped and has no `undefined` case, and `.null` crosses back as JS `null` (observably different: `String(null) === "null"`). So each native global is captured and replaced, in place, by a JS void wrapper emitted from `makePreamble(for:)`:
    `globalThis.notify = (function (send) { return function (detail) { send(detail); }; })(globalThis.notify);`
    That is the same "the seam cannot carry this shape, build it in JS" move `tools.*` itself makes, it adds no extra reachable global (so `HardeningTests`' exact-set pin still holds — and would have caught it if it did not), and it costs the preamble exactly one line.

    **Ordering and the flush.** `SandboxNoticeOutbox` chains each enqueued notice onto the previous delivery's `Task` handle — the same `latestDelivery` FIFO shape `ElevatingTool`'s funnel uses — so a loop's `progress("step 1"…"step 4")` reaches the sink in order while the snippet keeps running. `MultiTool.call(arguments:)` awaits `flush()` on **both** the success and the failure path, via a new `runCapturingOutcome(...)` that returns a `Result` instead of throwing. Without the flush a run could hand back its result before its last notice arrived.

    **`status(completionToken)` uses a zero-second `wait` as the mailbox's own lifecycle probe.** The mailbox exposes no per-token lookup: `wait(completionToken:seconds: 0)` resolves immediately to the retained terminal event for a settled token, reports `unknownToken` for an unknown one, and only reports `deadlineElapsed` for a run that is still parked — which the parked snapshot taken first already covered. The `deadlineElapsed` branch re-reads the snapshot (a run that parked between the two reads) so the reported object always carries a parked run's full field set, never a partial one.

    **Dead end worth not repeating:** rendering `ElicitationResponse` through its own `Codable` conformance drops `content` entirely for a decline/cancel (`encodeIfPresent`), so the snippet would see `undefined` rather than `null` and could not tell the three actions apart by shape. The response is built field by field instead, with `content: null` always present.

    **Vocabulary constraint recorded:** Router's `OperationEventKind` has exactly three cases, so `notify()` and `progress()` both post `.progress`-kind events — they differ only in which `ToolContext` capability delivers them (`post(_:)` vs `progress(_:)`), which is what the card and eventplan § "The capability contract" specify. A distinct notice kind, if ever wanted, lands in Router's enum, not here.
  timestamp: 2026-08-06T13:09:38.449164+00:00
- actor: claude-code
  id: 01kzbk4tw48m9wmnkjq5wcp3pj
  text: |-
    ### implement — changed
    - evidence: 9 files — Sources/FoundationModelsMultitool/MultiTool+SandboxGlobals.swift (new), Sources/FoundationModelsMultitool/Invocation/SandboxNoticeOutbox.swift (new), Sources/FoundationModelsMultitool/MultiTool.swift, Tests/FoundationModelsMultitoolTests/SandboxGlobalsTests.swift (new), Tests/FoundationModelsMultitoolTests/Fixtures/SandboxGlobalsFixtures.swift (new), Tests/FoundationModelsMultitoolTests/HardeningTests.swift, Tests/FoundationModelsMultitoolTests/HelpDocsTests.swift, Tests/FoundationModelsMultitoolTests/MultiToolExecutionTests.swift, README.md, docs/SECURITY.md
    - tests: `swift test` — 224 passed in 20 suites (was 199 in 19); the new "Sandbox globals" suite is 25 of them. `swift build` clean; the only warning is the known pre-existing, unrelated mlx-swift build-manifest "missing creator for mutated node".
    - flakiness: full suite run 6 times and the new suite in isolation 5 times, all green.
    - eventplan.md was NOT touched (its working-tree changes belong to a different task).
    - next: /review
  timestamp: 2026-08-06T13:10:33.604389+00:00
- actor: claude-code
  id: 01kzbke6qf3e9wz1gek2190cbn
  text: |-
    ### commit — changed
    - evidence: 5a57bee feat(sandbox): add status/wait/cancel/elicit/notify/progress globals (local commit, not pushed)
    - next: review
  timestamp: 2026-08-06T13:15:40.655605+00:00
- actor: claude-code
  id: 01kzbkpat9bar5z116sm9ysrc6
  text: |
    ### review — findings
    - evidence: review sha HEAD~1..HEAD — 1 finding, 1 confirmed, 0 refuted (9 validator passes attempted, 0 failed) — Tests/FoundationModelsMultitoolTests/Fixtures/SandboxGlobalsFixtures.swift:183
    - next: document the `post(_:)` sink method in SandboxGlobalsFixtures.swift, then re-review HEAD~1..HEAD
  timestamp: 2026-08-06T13:20:06.985004+00:00
- actor: claude-code
  id: 01kzbkq4qcasfwk52k4k17gp4x
  text: |-
    ### finish iteration 1 — findings
    - prerequisite: verified before starting — Router origin/main carries the elicitation reply surface (SessionMailbox.respond/complete at :477/:503, RoutedSession.respond/complete at :719/:740, ToolContext.elicit at :211); local checkout already at b5bf2ba from ^fhr27kf
    - implement: changed — new MultiTool+SandboxGlobals.swift (four promise-returning run-plane verbs + two sync void notices + named SandboxGlobalError) and Invocation/SandboxNoticeOutbox.swift (per-invocation FIFO delivery); all close over the same RunBinding MultiTool.call already captures. notify/progress evaluate to `undefined` via a JS void wrapper emitted from makePreamble, since InterpreterValue has no undefined case. Pins updated across HardeningTests, HelpDocsTests, MultiToolExecutionTests, README.md, docs/SECURITY.md. New SandboxGlobalsTests (25 tests) + fixtures driving a real SessionMailbox, real parked runs, and real elicitation round trips.
    - test: green — swift build clean, swift test 224/224 in 20 suites across 3 runs; SandboxGlobalsTests 25/25 across 10 isolated runs; no flakiness in the concurrency/elicitation cases
    - commit: 5a57bee feat(sandbox): add status/wait/cancel/elicit/notify/progress globals
    - review: findings — Tests/FoundationModelsMultitoolTests/Fixtures/SandboxGlobalsFixtures.swift:183 (the fixture's `post(_:)` OperationEventSink implementation lacks a doc comment; its behavior — appending events, responding to elicitations, completing URL-mode flows — is non-trivial and needs documenting independently of the actor-level docs). New code in this commit, so the never-refactor-existing-tests exception does not apply.
    - note: the reviewer's engine raised nothing against the three flagged decisions — the sibling-file placement of MultiTool+SandboxGlobals.swift, the undefined-returning JS void wrapper, or notify/progress sharing the .progress event kind. No global-leak finding for the preamble wrapper.
    - next: implement adds the doc comment, then re-test, re-commit, re-review
  timestamp: 2026-08-06T13:20:33.516287+00:00
- actor: claude-code
  id: 01kzbkyygnv8fq9xtqyzx70q46
  text: |-
    Pulled back from `review` to `doing` to address the one open finding.

    **The finding, addressed.** `Tests/FoundationModelsMultitoolTests/Fixtures/SandboxGlobalsFixtures.swift` — `ScriptedElicitationSink.post(_:)` now carries a doc comment. It documents the three things the actor-level docs did not cover for this method specifically: that it keeps *every* event and not only the elicitations (the non-elicitation events are what the notice-correlation tests assert on); that the answer is delivered from inside the post, before the method returns, which is the property that makes the round trips deterministic; and why only `.acceptedAwaitingCompletion` gets the second `complete(elicitationId:)` step — a URL-mode accept leaves the out-of-band flow open and the snippet parked, while a form accept, a decline, a cancel, or a rejected answer already settled the request.

    **Same cause, removed from the rest of the commit's new code.** A sweep of the two new test files and the two new source files for declarations with no preceding `///` found exactly one more: `SandboxGlobalError.description` in `Sources/FoundationModelsMultitool/MultiTool+SandboxGlobals.swift` — the same shape of miss, a protocol-conformance member whose behavior the type-level docs only gesture at. Documented it in the phrasing this repo already uses for that conformance (`ToolAPIRenderer`, `MultiToolBuilder`: "A human-readable description of the error, satisfying `CustomStringConvertible`"), plus the reason the text reads as repair instructions rather than as a diagnosis. Nothing else in either new test file or either new source file is undocumented; the `@Test` methods are exempt under the Swift documentation rule's "There is NO NEED to put doc comments on test methods."

    **Not touched, deliberately.** `AmbientUpstreamSink.post(_:)` (`Sources/.../Invocation/RunBinding.swift`) and `RecordingEventSink.post(_:)` (`Tests/.../Fixtures/RunBindingFixtures.swift`) have the same gap but are pre-existing code from an earlier commit, outside this review's scope.

    Docs-only change: no behavior, no signature, no test moved.
  timestamp: 2026-08-06T13:24:49.301145+00:00
- actor: claude-code
  id: 01kzbkz9ms42mxrp8y269bv2cr
  text: |-
    ### implement — changed
    - evidence: 2 files — Tests/FoundationModelsMultitoolTests/Fixtures/SandboxGlobalsFixtures.swift (+21), Sources/FoundationModelsMultitool/MultiTool+SandboxGlobals.swift (+6)
    - tests: `swift build` clean; `swift test` — 224 passed in 20 suites, 0 failures. The only warning is the known pre-existing, unrelated mlx-swift build-manifest "missing creator for mutated node".
    - findings: the one open finding (SandboxGlobalsFixtures.swift `post(_:)`) is addressed and its checkbox flipped to `- [x]`; 0 open remain.
    - eventplan.md was NOT touched (its working-tree changes belong to a different task and predate this pass).
    - next: /review — re-review the change
  timestamp: 2026-08-06T13:25:00.697083+00:00
- actor: claude-code
  id: 01kzbm8p7yxejsx8zhxd653tzx
  text: |-
    ### review — clean
    - evidence: `review sha HEAD~1..HEAD` (8bf6f01) — 0 findings, 0 confirmed, 0 refuted, 9 validators attempted, 0 failed; prior 2026-08-06 08:16 finding checked
    - next: none — task moved to done
  timestamp: 2026-08-06T13:30:08.510752+00:00
- actor: claude-code
  id: 01kzbm96z8w618gfyq9en9ehr4
  text: |-
    ### finish iteration 2 — clean
    - implement: changed — 2 files, documentation only. Added the doc comment the finding asked for on ScriptedElicitationSink.post(_:), and per the remove-the-cause-from-the-whole-file rule swept the rest of the commit's new code, finding and documenting one more undocumented protocol-conformance member (SandboxGlobalError.description). Two pre-existing same-shape gaps from the earlier ^fhr27kf commit were deliberately left as out of scope.
    - test: green — swift build clean, swift test 224/224 in 20 suites
    - commit: 8bf6f01 docs(sandbox-globals): document ScriptedElicitationSink.post and SandboxGlobalError.description
    - review: clean — `review sha HEAD~1..HEAD`, 0 findings, 9 validators attempted, 0 failed; the iteration-1 finding is checked off
    - outcome: task moved to done
  timestamp: 2026-08-06T13:30:25.640771+00:00
depends_on:
- 01KZ6N3KMERCMS4DCMEFHR27KF
position_column: done
position_ordinal: ae80
title: '[MultiTool] Sandbox globals: status, wait, cancel, elicit, notify, progress'
---
CROSS-BOARD PREREQUISITE: Router's elicitation reply surface (`respond(elicitationId:response:)` / `complete(elicitationId:)`, on ../FoundationModelsRouter's own kanban board, tagged `router-first`) must be on Router `main` — the elicit round-trip tests here drive it.

Repo: this repo. Basis: eventplan.md §"The sandbox globals" and the capability-contract table.

## What
Add six top-level globals to the snippet sandbox (installed the way `help`/`docs` are, in `MultiTool.makeHelpDocsHostFunctions`-style factories in `MultiTool.swift`):
- `status(completionToken?)` — promise; with a token, that run's lifecycle; with no argument, lists each pending run from the session mailbox: token, op, latest progress. Run plane only — never bulk output.
- `wait(completionToken, seconds)` — promise; awaits settlement via the mailbox with a deadline. Returns the terminal event's `detail` — the capped output tail plus the run's identifier (the single `wait()` contract fixed in the SessionMailbox task) — never a capability's full store.
- `cancel(completionToken)` — promise; invokes the honest canceler, returns the honest outcome.
- `elicit(request)` — promise; parks through `ToolContext.elicit` on the ambient context (same elevation path as every other call; the model itself can stop mid-snippet for a user answer). String shorthand `elicit("question")` maps to a form-mode message-only request.
- `notify(detail)` / `progress(detail)` — synchronous, void: enqueue through `ToolContext.post`/`progress` and continue; the bridge flushes them. The outbox coalesces snippet `progress()` like any tool's progress event.
- The one-rule contract holds: calls that do Swift effects return promises (`tools.*`, `elicit`, `wait`, `status`, `cancel`); `help()`/`docs()` stay sync reads; `notify()`/`progress()` are sync void.
- No-ambient-context degradation (a `MultiTool` constructed and called directly, outside any Router session — exactly how the existing unit suites run): the four promise-returning run-plane globals (`status`, `wait`, `cancel`, `elicit`) reject with a precise, named, model-repairable error ("no session context — this run has no run plane") and never trap; `notify`/`progress` are silent no-ops (consistent with nil-context `post`).

Surface text and pins:
- `MultiTool.description` gains one sentence naming the ambient globals (`elicit()`, `notify()`, `progress()`, and the `status()`/`wait()`/`cancel()` run verbs). No `findAPIs` entries for globals — they are always present, not discoverable/absent.
- Update the three globals pins: `HardeningTests` lines 187 and 193 (exact-set and README set-equality via `rawJSContextGlobals()`), `HelpDocsTests` line 114; prose lists at `README.md:124-135` and `docs/SECURITY.md:16-26`.

## Acceptance Criteria
- [x] All six globals reachable in a fresh run; `findAPIs`/`help()` do NOT list them
- [x] `status()` with no argument lists a parked run's token, op, and latest progress; `wait` returns the terminal event's bounded `detail` with the run identifier; `cancel` returns the honest outcome
- [x] `elicit` parks the snippet and resumes with the response when the session's `respond(elicitationId:response:)` fires; decline and cancel actions surface distinctly to the snippet
- [x] `notify`/`progress` return undefined synchronously and their events reach the sink with the run's correlation
- [x] Each run-plane global called on a MultiTool with no ambient context rejects with a named repairable error and never traps; `notify`/`progress` no-op
- [x] HardeningTests/HelpDocsTests/README/SECURITY pins all updated and green

## Tests
- [x] New `Tests/FoundationModelsMultitoolTests/SandboxGlobalsTests.swift`: per-global behavior incl. promise-vs-sync contract; `status()` no-arg listing; bounded `wait()` result; elicit accept/decline/cancel round trips against a scripted mailbox; no-context rejection matrix for all six globals
- [x] Updated `HardeningTests` + `HelpDocsTests` + description pins
- [x] `swift test` green

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass. #phase-1

## Review Findings (2026-08-06 08:16)

- [x] `Tests/FoundationModelsMultitoolTests/Fixtures/SandboxGlobalsFixtures.swift:183` — Public async method `post(_:)` implementing `OperationEventSink` protocol lacks documentation. The actor-level docs (lines 141-153) describe the type's overall purpose but not this method's specific behavior (it appends events, responds to elicitations, and completes URL-mode flows), which is non-trivial and should be documented independently. Add a doc comment above line 183 explaining the method's behavior, e.g.: `/// Records the event and automatically responds to any elicitation it carries, completing URL-mode flows as needed.`.


## Superseded in part — 2026-08-11

The `wait` **global** is being removed from the sandbox. `status()`, `cancel()`, `elicit()`, `notify()` and `progress()` all stand — a query, a command, a question for a person, and two notices, none of them a bet on a duration.

`wait()` was different in kind: it asked the model to predict how long another party's work takes, and every value it can write is wrong. It is replaced by a mounted `wait` tool that takes an *intent to block* plus a caller-chosen bound — **`^ddgjps6`**.

So this card's coverage of the other five globals is unaffected; its `wait` half is replaced. Note the doc block at `MultiTool+SandboxGlobals.swift:195` still teaches `await wait(token, 30)` and must go with the binding.
