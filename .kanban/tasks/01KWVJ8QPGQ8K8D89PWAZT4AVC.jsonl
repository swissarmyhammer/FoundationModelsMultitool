{"id":"01KWVJ8QPG3NH19Z115SH4KFE0","timestamp":"2026-07-06T11:15:58.288829Z","op":"create","item_id":"01KWVJ8QPGQ8K8D89PWAZT4AVC","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,31 @@\n+---\n+position_column: todo\n+position_ordinal: '8180'\n+title: Add a canonical-usage ExamplesTests suite, mirroring FoundationModelsRouter/FoundationModelsMetadataRegistry's living-documentation pattern\n+---\n+## What\n+Both sibling packages give consumers copy-pasteable, tested \"how do I…\" examples:\n+- `FoundationModelsRouter`'s `Tests/FoundationModelsRouterTests/ExamplesTests.swift` is a `@Suite(\"Examples: canonical usage of the public API\")` — each `@Test` is a self-contained example whose body reads like real consumer code (author a `ProfileDefinition`, resolve it, drive the resolved profile), running fully offline via injected stubs so it stays green in CI with no network/GPU.\n+- `FoundationModelsMetadataRegistry` has an analogous `Tests/FoundationModelsMetadataRegistryTests/ExamplesSmokeTests.swift` plus a whole `Examples/` directory of standalone runnable programs.\n+\n+`FoundationModelsMultitool` has **no equivalent**. Its only runnable sample is `Sources/multitool-cli` (one CLI demo), and there is no offline, in-unit-test-target \"canonical usage\" suite demonstrating the library's own core API end to end — most notably, there is no clear, concrete, testable example of the **`findAPIs` → `runCode` discovery-then-call handoff**: how a `findAPIs(task)` call's result (rendered tool signatures/docs/examples) is meant to inform the very next `runCode` snippet the model writes. Found while walking through `MultiToolAgent`'s instruction assembly with a user who correctly pointed out neither the prompt instructions nor the test suite make that handoff concrete anywhere.\n+\n+Add `Tests/FoundationModelsMultitoolTests/ExamplesTests.swift`, a `@Suite(\"Examples: canonical usage of the public API\")`, entirely offline (no live model, no network) using the scripted fixtures already in `Tests/FoundationModelsMultitoolTests/Fixtures/MultiToolAgentFixtures.swift` — `ScriptedAgentSession`, `RootSessionRespondCalledDirectlySession`, `makeScriptedSelectionSearcher(registry:root:)`, `TripCitiesTool` — plus `MultiTool.Builder` (`Sources/FoundationModelsMultitool/Surface/MultiToolBuilder.swift`) and `MultiToolAgent`'s internal test-facing initializer (`init(registry:session:findAPISearcher:turnFormat:directTools:)`, `Sources/FoundationModelsMultitool/Agent/MultiToolAgent.swift`).\n+\n+At minimum, include these three canonical examples, each a self-contained `@Test`:\n+1. **\"Author a catalog with `MultiTool.Builder`: standalone and grouped tools\"** — build a small `APISurface` via `.addTool(_:)` and `.addGroup(named:_:)`, then assert the rendered surface's shape (e.g. via `registry.surface.entries`/`.source`) so a reader sees exactly how to stand up a catalog.\n+2. **\"Run a direct `runCode` turn end to end\"** — wire a `MultiToolAgent` (test-facing init) with a scripted main session that emits one `runCode` turn calling a fixture tool, then a `final` turn, and assert the returned answer — the simplest possible \"how do I drive the agent\" example.\n+3. **\"The `findAPIs` → `runCode` discovery-then-call handoff\"** — the example this task exists for: script `findAPISearcher` (via `makeScriptedSelectionSearcher`/`RootSessionRespondCalledDirectlySession`) to select a specific tool by id, script the main session to first call `findAPIs(task: \"...\")`, assert the *exact* discovery text fed back into the transcript names that tool's real call path and signature, then script the main session's next turn to issue a `runCode` snippet using that discovered signature, and assert the final answer — making the handoff between discovery and invocation concrete and copy-pasteable for the first time anywhere in this repo.\n+\n+## Acceptance Criteria\n+- [ ] `Tests/FoundationModelsMultitoolTests/ExamplesTests.swift` exists as a new file, `@Suite(\"Examples: canonical usage of the public API\")`, with at least the three examples above.\n+- [ ] Every example runs fully offline (no `MULTITOOL_INTEGRATION` gate, no live model, no network) — reuses existing scripted fixtures from `MultiToolAgentFixtures.swift` rather than inventing new ones, unless a genuine gap requires a small addition there.\n+- [ ] The `findAPIs` → `runCode` example explicitly asserts on the discovery text handed back to the main session (not just the final answer), so it documents *what the model actually sees* between the two steps.\n+- [ ] `swift build` and full `swift test` remain green.\n+\n+## Tests\n+- [ ] The new `ExamplesTests.swift` suite itself, run via `swift test --filter ExamplesTests`, all `@Test`s pass.\n+- [ ] Full `swift test` passes with no regressions.\n+\n+## Workflow\n+- Use `/tdd` — write each example test against the real (scripted) `MultiToolAgent`/`MetadataSearcher` APIs, watch it fail/compile-error first if it exercises a new fixture combination, then adjust until green. No production code changes are expected; if the examples reveal a genuine API/documentation gap in `MultiToolAgent`/`FindAPITool` while writing them, note it as a follow-up task rather than expanding this one's scope.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,31 +0,0 @@\n----\n-position_column: todo\n-position_ordinal: '8180'\n-title: Add a canonical-usage ExamplesTests suite, mirroring FoundationModelsRouter/FoundationModelsMetadataRegistry's living-documentation pattern\n----\n-## What\n-Both sibling packages give consumers copy-pasteable, tested \"how do I…\" examples:\n-- `FoundationModelsRouter`'s `Tests/FoundationModelsRouterTests/ExamplesTests.swift` is a `@Suite(\"Examples: canonical usage of the public API\")` — each `@Test` is a self-contained example whose body reads like real consumer code (author a `ProfileDefinition`, resolve it, drive the resolved profile), running fully offline via injected stubs so it stays green in CI with no network/GPU.\n-- `FoundationModelsMetadataRegistry` has an analogous `Tests/FoundationModelsMetadataRegistryTests/ExamplesSmokeTests.swift` plus a whole `Examples/` directory of standalone runnable programs.\n-\n-`FoundationModelsMultitool` has **no equivalent**. Its only runnable sample is `Sources/multitool-cli` (one CLI demo), and there is no offline, in-unit-test-target \"canonical usage\" suite demonstrating the library's own core API end to end — most notably, there is no clear, concrete, testable example of the **`findAPIs` → `runCode` discovery-then-call handoff**: how a `findAPIs(task)` call's result (rendered tool signatures/docs/examples) is meant to inform the very next `runCode` snippet the model writes. Found while walking through `MultiToolAgent`'s instruction assembly with a user who correctly pointed out neither the prompt instructions nor the test suite make that handoff concrete anywhere.\n-\n-Add `Tests/FoundationModelsMultitoolTests/ExamplesTests.swift`, a `@Suite(\"Examples: canonical usage of the public API\")`, entirely offline (no live model, no network) using the scripted fixtures already in `Tests/FoundationModelsMultitoolTests/Fixtures/MultiToolAgentFixtures.swift` — `ScriptedAgentSession`, `RootSessionRespondCalledDirectlySession`, `makeScriptedSelectionSearcher(registry:root:)`, `TripCitiesTool` — plus `MultiTool.Builder` (`Sources/FoundationModelsMultitool/Surface/MultiToolBuilder.swift`) and `MultiToolAgent`'s internal test-facing initializer (`init(registry:session:findAPISearcher:turnFormat:directTools:)`, `Sources/FoundationModelsMultitool/Agent/MultiToolAgent.swift`).\n-\n-At minimum, include these three canonical examples, each a self-contained `@Test`:\n-1. **\"Author a catalog with `MultiTool.Builder`: standalone and grouped tools\"** — build a small `APISurface` via `.addTool(_:)` and `.addGroup(named:_:)`, then assert the rendered surface's shape (e.g. via `registry.surface.entries`/`.source`) so a reader sees exactly how to stand up a catalog.\n-2. **\"Run a direct `runCode` turn end to end\"** — wire a `MultiToolAgent` (test-facing init) with a scripted main session that emits one `runCode` turn calling a fixture tool, then a `final` turn, and assert the returned answer — the simplest possible \"how do I drive the agent\" example.\n-3. **\"The `findAPIs` → `runCode` discovery-then-call handoff\"** — the example this task exists for: script `findAPISearcher` (via `makeScriptedSelectionSearcher`/`RootSessionRespondCalledDirectlySession`) to select a specific tool by id, script the main session to first call `findAPIs(task: \"...\")`, assert the *exact* discovery text fed back into the transcript names that tool's real call path and signature, then script the main session's next turn to issue a `runCode` snippet using that discovered signature, and assert the final answer — making the handoff between discovery and invocation concrete and copy-pasteable for the first time anywhere in this repo.\n-\n-## Acceptance Criteria\n-- [ ] `Tests/FoundationModelsMultitoolTests/ExamplesTests.swift` exists as a new file, `@Suite(\"Examples: canonical usage of the public API\")`, with at least the three examples above.\n-- [ ] Every example runs fully offline (no `MULTITOOL_INTEGRATION` gate, no live model, no network) — reuses existing scripted fixtures from `MultiToolAgentFixtures.swift` rather than inventing new ones, unless a genuine gap requires a small addition there.\n-- [ ] The `findAPIs` → `runCode` example explicitly asserts on the discovery text handed back to the main session (not just the final answer), so it documents *what the model actually sees* between the two steps.\n-- [ ] `swift build` and full `swift test` remain green.\n-\n-## Tests\n-- [ ] The new `ExamplesTests.swift` suite itself, run via `swift test --filter ExamplesTests`, all `@Test`s pass.\n-- [ ] Full `swift test` passes with no regressions.\n-\n-## Workflow\n-- Use `/tdd` — write each example test against the real (scripted) `MultiToolAgent`/`MetadataSearcher` APIs, watch it fail/compile-error first if it exercises a new fixture combination, then adjust until green. No production code changes are expected; if the examples reveal a genuine API/documentation gap in `MultiToolAgent`/`FindAPITool` while writing them, note it as a follow-up task rather than expanding this one's scope.\n","transaction_id":null}
{"id":"01KWVNZ2WJN82KKG4RP7HHMW4J","timestamp":"2026-07-06T12:20:36.370199Z","op":"update","item_id":"01KWVJ8QPGQ8K8D89PWAZT4AVC","forward_patch":"--- original\n+++ modified\n@@ -4,28 +4,27 @@\n title: Add a canonical-usage ExamplesTests suite, mirroring FoundationModelsRouter/FoundationModelsMetadataRegistry's living-documentation pattern\n ---\n ## What\n+**Rescoped 2026-07-06**: originally written around `MultiToolAgent`'s scripted-session fixtures (see comment history), before the decision to remove `MultiToolAgent` entirely in favor of Apple's native `LanguageModelSession(tools:)` tool-calling (see board — depends on `4aveepp`, `f329qvr`). This task now targets the *new* design.\n+\n Both sibling packages give consumers copy-pasteable, tested \"how do I…\" examples:\n-- `FoundationModelsRouter`'s `Tests/FoundationModelsRouterTests/ExamplesTests.swift` is a `@Suite(\"Examples: canonical usage of the public API\")` — each `@Test` is a self-contained example whose body reads like real consumer code (author a `ProfileDefinition`, resolve it, drive the resolved profile), running fully offline via injected stubs so it stays green in CI with no network/GPU.\n-- `FoundationModelsMetadataRegistry` has an analogous `Tests/FoundationModelsMetadataRegistryTests/ExamplesSmokeTests.swift` plus a whole `Examples/` directory of standalone runnable programs.\n-\n-`FoundationModelsMultitool` has **no equivalent**. Its only runnable sample is `Sources/multitool-cli` (one CLI demo), and there is no offline, in-unit-test-target \"canonical usage\" suite demonstrating the library's own core API end to end — most notably, there is no clear, concrete, testable example of the **`findAPIs` → `runCode` discovery-then-call handoff**: how a `findAPIs(task)` call's result (rendered tool signatures/docs/examples) is meant to inform the very next `runCode` snippet the model writes. Found while walking through `MultiToolAgent`'s instruction assembly with a user who correctly pointed out neither the prompt instructions nor the test suite make that handoff concrete anywhere.\n+- `FoundationModelsRouter`'s `Tests/FoundationModelsRouterTests/ExamplesTests.swift` is a `@Suite(\"Examples: canonical usage of the public API\")` — each `@Test` is a self-contained example whose body reads like real consumer code, running fully offline via injected stubs so it stays green in CI with no network/GPU.\n+- `FoundationModelsMetadataRegistry` has an analogous `ExamplesSmokeTests.swift` plus a whole `Examples/` directory.\n\n-Add `Tests/FoundationModelsMultitoolTests/ExamplesTests.swift`, a `@Suite(\"Examples: canonical usage of the public API\")`, entirely offline (no live model, no network) using the scripted fixtures already in `Tests/FoundationModelsMultitoolTests/Fixtures/MultiToolAgentFixtures.swift` — `ScriptedAgentSession`, `RootSessionRespondCalledDirectlySession`, `makeScriptedSelectionSearcher(registry:root:)`, `TripCitiesTool` — plus `MultiTool.Builder` (`Sources/FoundationModelsMultitool/Surface/MultiToolBuilder.swift`) and `MultiToolAgent`'s internal test-facing initializer (`init(registry:session:findAPISearcher:turnFormat:directTools:)`, `Sources/FoundationModelsMultitool/Agent/MultiToolAgent.swift`).\n+`FoundationModelsMultitool` still has no equivalent. Add `Tests/FoundationModelsMultitoolTests/ExamplesTests.swift`, a `@Suite(\"Examples: canonical usage of the public API\")`, demonstrating the library's core API end to end using the **new** design:\n\n-At minimum, include these three canonical examples, each a self-contained `@Test`:\n-1. **\"Author a catalog with `MultiTool.Builder`: standalone and grouped tools\"** — build a small `APISurface` via `.addTool(_:)` and `.addGroup(named:_:)`, then assert the rendered surface's shape (e.g. via `registry.surface.entries`/`.source`) so a reader sees exactly how to stand up a catalog.\n-2. **\"Run a direct `runCode` turn end to end\"** — wire a `MultiToolAgent` (test-facing init) with a scripted main session that emits one `runCode` turn calling a fixture tool, then a `final` turn, and assert the returned answer — the simplest possible \"how do I drive the agent\" example.\n-3. **\"The `findAPIs` → `runCode` discovery-then-call handoff\"** — the example this task exists for: script `findAPISearcher` (via `makeScriptedSelectionSearcher`/`RootSessionRespondCalledDirectlySession`) to select a specific tool by id, script the main session to first call `findAPIs(task: \"...\")`, assert the *exact* discovery text fed back into the transcript names that tool's real call path and signature, then script the main session's next turn to issue a `runCode` snippet using that discovered signature, and assert the final answer — making the handoff between discovery and invocation concrete and copy-pasteable for the first time anywhere in this repo.\n+1. **\"Author a catalog with `MultiTool.Builder`: standalone and grouped tools\"** — unchanged from the original scope; build a small `APISurface` via `.addTool(_:)`/`.addGroup(named:_:)`, assert the rendered surface's shape.\n+2. **\"Register `MultiTool` directly with Apple's `LanguageModelSession`\"** — the simplest real integration: construct `MultiTool(registry:)`, hand it to a `LanguageModelSession(tools: [multiTool])` (using whatever offline-stubbable seam `MLXLanguageModel`/`LanguageModelSession` testing requires — check how `FoundationModelsRouter`'s own `ExamplesTests.swift` stubs its seams, e.g. `ExampleHarness`'s canned-container pattern, for a model to follow), and show a tool call round-tripping.\n+3. **\"The `findAPIs` → `runCode` discovery-then-call handoff, via native tool-calling\"** — script `findAPIsTool`'s selection tier to pick a specific tool by id, register both `multiTool` and `findAPIsTool` with a `LanguageModelSession`, and show (script) the model calling `findAPIs` then `runCode` in sequence — this is the concrete, tested version of the exact worked example already written out in chat with the user (a `findAPIs(\"...\")` call returning a rendered block, followed by a `runCode` call using the discovered, properly-qualified signature). This example inherently depends on `mlx-swift-lm`'s multi-turn tool-calling fix (`qp8q4h9`) if driven against a *real* model — for the offline example test, script/stub the executor directly so the example doesn't require live hardware or the upstream fix to be green in CI (the example documents the *intended* usage regardless of the current live-hardware caveat).\n\n ## Acceptance Criteria\n-- [ ] `Tests/FoundationModelsMultitoolTests/ExamplesTests.swift` exists as a new file, `@Suite(\"Examples: canonical usage of the public API\")`, with at least the three examples above.\n-- [ ] Every example runs fully offline (no `MULTITOOL_INTEGRATION` gate, no live model, no network) — reuses existing scripted fixtures from `MultiToolAgentFixtures.swift` rather than inventing new ones, unless a genuine gap requires a small addition there.\n-- [ ] The `findAPIs` → `runCode` example explicitly asserts on the discovery text handed back to the main session (not just the final answer), so it documents *what the model actually sees* between the two steps.\n+- [ ] `Tests/FoundationModelsMultitoolTests/ExamplesTests.swift` exists, `@Suite(\"Examples: canonical usage of the public API\")`, with at least the three examples above.\n+- [ ] Every example runs fully offline (no live model, no network, no `MULTITOOL_INTEGRATION` gate) via stubbed seams, matching the sibling packages' own `ExamplesTests` pattern.\n+- [ ] The `findAPIs` → `runCode` example explicitly asserts on the discovery text handed back (not just the final answer), documenting what the model actually sees between the two steps, including a grouped tool's properly-qualified example call (per task `12rtn85`'s fix).\n - [ ] `swift build` and full `swift test` remain green.\n\n ## Tests\n-- [ ] The new `ExamplesTests.swift` suite itself, run via `swift test --filter ExamplesTests`, all `@Test`s pass.\n+- [ ] The new `ExamplesTests.swift` suite itself, `swift test --filter ExamplesTests`, all `@Test`s pass.\n - [ ] Full `swift test` passes with no regressions.\n\n ## Workflow\n-- Use `/tdd` — write each example test against the real (scripted) `MultiToolAgent`/`MetadataSearcher` APIs, watch it fail/compile-error first if it exercises a new fixture combination, then adjust until green. No production code changes are expected; if the examples reveal a genuine API/documentation gap in `MultiToolAgent`/`FindAPITool` while writing them, note it as a follow-up task rather than expanding this one's scope.\n+- Use `/tdd` — write each example test against the real `MultiTool`/`findAPIsTool`/`LanguageModelSession` APIs (with offline stubs), watch it fail first, then adjust until green.\n","reverse_patch":"--- original\n+++ modified\n@@ -4,27 +4,28 @@\n title: Add a canonical-usage ExamplesTests suite, mirroring FoundationModelsRouter/FoundationModelsMetadataRegistry's living-documentation pattern\n ---\n ## What\n-**Rescoped 2026-07-06**: originally written around `MultiToolAgent`'s scripted-session fixtures (see comment history), before the decision to remove `MultiToolAgent` entirely in favor of Apple's native `LanguageModelSession(tools:)` tool-calling (see board — depends on `4aveepp`, `f329qvr`). This task now targets the *new* design.\n-\n Both sibling packages give consumers copy-pasteable, tested \"how do I…\" examples:\n-- `FoundationModelsRouter`'s `Tests/FoundationModelsRouterTests/ExamplesTests.swift` is a `@Suite(\"Examples: canonical usage of the public API\")` — each `@Test` is a self-contained example whose body reads like real consumer code, running fully offline via injected stubs so it stays green in CI with no network/GPU.\n-- `FoundationModelsMetadataRegistry` has an analogous `ExamplesSmokeTests.swift` plus a whole `Examples/` directory.\n+- `FoundationModelsRouter`'s `Tests/FoundationModelsRouterTests/ExamplesTests.swift` is a `@Suite(\"Examples: canonical usage of the public API\")` — each `@Test` is a self-contained example whose body reads like real consumer code (author a `ProfileDefinition`, resolve it, drive the resolved profile), running fully offline via injected stubs so it stays green in CI with no network/GPU.\n+- `FoundationModelsMetadataRegistry` has an analogous `Tests/FoundationModelsMetadataRegistryTests/ExamplesSmokeTests.swift` plus a whole `Examples/` directory of standalone runnable programs.\n\n-`FoundationModelsMultitool` still has no equivalent. Add `Tests/FoundationModelsMultitoolTests/ExamplesTests.swift`, a `@Suite(\"Examples: canonical usage of the public API\")`, demonstrating the library's core API end to end using the **new** design:\n+`FoundationModelsMultitool` has **no equivalent**. Its only runnable sample is `Sources/multitool-cli` (one CLI demo), and there is no offline, in-unit-test-target \"canonical usage\" suite demonstrating the library's own core API end to end — most notably, there is no clear, concrete, testable example of the **`findAPIs` → `runCode` discovery-then-call handoff**: how a `findAPIs(task)` call's result (rendered tool signatures/docs/examples) is meant to inform the very next `runCode` snippet the model writes. Found while walking through `MultiToolAgent`'s instruction assembly with a user who correctly pointed out neither the prompt instructions nor the test suite make that handoff concrete anywhere.\n\n-1. **\"Author a catalog with `MultiTool.Builder`: standalone and grouped tools\"** — unchanged from the original scope; build a small `APISurface` via `.addTool(_:)`/`.addGroup(named:_:)`, assert the rendered surface's shape.\n-2. **\"Register `MultiTool` directly with Apple's `LanguageModelSession`\"** — the simplest real integration: construct `MultiTool(registry:)`, hand it to a `LanguageModelSession(tools: [multiTool])` (using whatever offline-stubbable seam `MLXLanguageModel`/`LanguageModelSession` testing requires — check how `FoundationModelsRouter`'s own `ExamplesTests.swift` stubs its seams, e.g. `ExampleHarness`'s canned-container pattern, for a model to follow), and show a tool call round-tripping.\n-3. **\"The `findAPIs` → `runCode` discovery-then-call handoff, via native tool-calling\"** — script `findAPIsTool`'s selection tier to pick a specific tool by id, register both `multiTool` and `findAPIsTool` with a `LanguageModelSession`, and show (script) the model calling `findAPIs` then `runCode` in sequence — this is the concrete, tested version of the exact worked example already written out in chat with the user (a `findAPIs(\"...\")` call returning a rendered block, followed by a `runCode` call using the discovered, properly-qualified signature). This example inherently depends on `mlx-swift-lm`'s multi-turn tool-calling fix (`qp8q4h9`) if driven against a *real* model — for the offline example test, script/stub the executor directly so the example doesn't require live hardware or the upstream fix to be green in CI (the example documents the *intended* usage regardless of the current live-hardware caveat).\n+Add `Tests/FoundationModelsMultitoolTests/ExamplesTests.swift`, a `@Suite(\"Examples: canonical usage of the public API\")`, entirely offline (no live model, no network) using the scripted fixtures already in `Tests/FoundationModelsMultitoolTests/Fixtures/MultiToolAgentFixtures.swift` — `ScriptedAgentSession`, `RootSessionRespondCalledDirectlySession`, `makeScriptedSelectionSearcher(registry:root:)`, `TripCitiesTool` — plus `MultiTool.Builder` (`Sources/FoundationModelsMultitool/Surface/MultiToolBuilder.swift`) and `MultiToolAgent`'s internal test-facing initializer (`init(registry:session:findAPISearcher:turnFormat:directTools:)`, `Sources/FoundationModelsMultitool/Agent/MultiToolAgent.swift`).\n\n+At minimum, include these three canonical examples, each a self-contained `@Test`:\n+1. **\"Author a catalog with `MultiTool.Builder`: standalone and grouped tools\"** — build a small `APISurface` via `.addTool(_:)` and `.addGroup(named:_:)`, then assert the rendered surface's shape (e.g. via `registry.surface.entries`/`.source`) so a reader sees exactly how to stand up a catalog.\n+2. **\"Run a direct `runCode` turn end to end\"** — wire a `MultiToolAgent` (test-facing init) with a scripted main session that emits one `runCode` turn calling a fixture tool, then a `final` turn, and assert the returned answer — the simplest possible \"how do I drive the agent\" example.\n+3. **\"The `findAPIs` → `runCode` discovery-then-call handoff\"** — the example this task exists for: script `findAPISearcher` (via `makeScriptedSelectionSearcher`/`RootSessionRespondCalledDirectlySession`) to select a specific tool by id, script the main session to first call `findAPIs(task: \"...\")`, assert the *exact* discovery text fed back into the transcript names that tool's real call path and signature, then script the main session's next turn to issue a `runCode` snippet using that discovered signature, and assert the final answer — making the handoff between discovery and invocation concrete and copy-pasteable for the first time anywhere in this repo.\n+\n ## Acceptance Criteria\n-- [ ] `Tests/FoundationModelsMultitoolTests/ExamplesTests.swift` exists, `@Suite(\"Examples: canonical usage of the public API\")`, with at least the three examples above.\n-- [ ] Every example runs fully offline (no live model, no network, no `MULTITOOL_INTEGRATION` gate) via stubbed seams, matching the sibling packages' own `ExamplesTests` pattern.\n-- [ ] The `findAPIs` → `runCode` example explicitly asserts on the discovery text handed back (not just the final answer), documenting what the model actually sees between the two steps, including a grouped tool's properly-qualified example call (per task `12rtn85`'s fix).\n+- [ ] `Tests/FoundationModelsMultitoolTests/ExamplesTests.swift` exists as a new file, `@Suite(\"Examples: canonical usage of the public API\")`, with at least the three examples above.\n+- [ ] Every example runs fully offline (no `MULTITOOL_INTEGRATION` gate, no live model, no network) — reuses existing scripted fixtures from `MultiToolAgentFixtures.swift` rather than inventing new ones, unless a genuine gap requires a small addition there.\n+- [ ] The `findAPIs` → `runCode` example explicitly asserts on the discovery text handed back to the main session (not just the final answer), so it documents *what the model actually sees* between the two steps.\n - [ ] `swift build` and full `swift test` remain green.\n\n ## Tests\n-- [ ] The new `ExamplesTests.swift` suite itself, `swift test --filter ExamplesTests`, all `@Test`s pass.\n+- [ ] The new `ExamplesTests.swift` suite itself, run via `swift test --filter ExamplesTests`, all `@Test`s pass.\n - [ ] Full `swift test` passes with no regressions.\n\n ## Workflow\n-- Use `/tdd` — write each example test against the real `MultiTool`/`findAPIsTool`/`LanguageModelSession` APIs (with offline stubs), watch it fail first, then adjust until green.\n+- Use `/tdd` — write each example test against the real (scripted) `MultiToolAgent`/`MetadataSearcher` APIs, watch it fail/compile-error first if it exercises a new fixture combination, then adjust until green. No production code changes are expected; if the examples reveal a genuine API/documentation gap in `MultiToolAgent`/`FindAPITool` while writing them, note it as a follow-up task rather than expanding this one's scope.\n","transaction_id":null}
{"id":"01KWVNZ73VZT3E1ZN052M1KCKT","timestamp":"2026-07-06T12:20:40.699902Z","op":"update","item_id":"01KWVJ8QPGQ8K8D89PWAZT4AVC","forward_patch":"--- original\n+++ modified\n@@ -1,4 +1,7 @@\n ---\n+depends_on:\n+- 01KWVNTEAPVS13BB8H04AVEEPP\n+- 01KWVNVV79AAK6FDHRJF329QVR\n position_column: todo\n position_ordinal: '8180'\n title: Add a canonical-usage ExamplesTests suite, mirroring FoundationModelsRouter/FoundationModelsMetadataRegistry's living-documentation pattern\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,4 @@\n ---\n-depends_on:\n-- 01KWVNTEAPVS13BB8H04AVEEPP\n-- 01KWVNVV79AAK6FDHRJF329QVR\n position_column: todo\n position_ordinal: '8180'\n title: Add a canonical-usage ExamplesTests suite, mirroring FoundationModelsRouter/FoundationModelsMetadataRegistry's living-documentation pattern\n","transaction_id":null}
