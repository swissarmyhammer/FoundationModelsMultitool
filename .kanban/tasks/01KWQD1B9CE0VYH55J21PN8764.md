---
comments:
- actor: wballard
  id: 01kwqmy2qkgcbr1y8ax7bhdb5c
  text: Fixed as a side effect of task exbtj1n (gated integration suite), which needed the ungated `swift test` green to satisfy its own acceptance criteria. Restored a "### Injected globals" section to README.md (under a new "## Security model" heading, linking to docs/SECURITY.md for full detail) rather than repointing the test at SECURITY.md — this keeps the machine-checked README<->code sync anchored at the root README as originally designed. `swift test --filter HardeningTests` and full `swift test` now pass. Leaving this task for review/closure since the fix already landed on main via exbtj1n's work.
  timestamp: 2026-07-04T22:45:37.139547+00:00
- actor: wballard
  id: 01kwqtr32zmc3k29fpxjbk4fwj
  text: '/implement pass: independently re-verified the fix from exbtj1n rather than trusting the prior comment. Confirmed README.md''s "## Security model" (line 63) / "### Injected globals" (line 68) section is present with exactly console, tools, help, docs, linking to docs/SECURITY.md for detail — HardeningTests.readmeInjectedGlobals() parses this unmodified. Ran fresh: `swift test --filter HardeningTests` = 17/17 pass; full `swift test` = 250/250 pass (exit 0), plus the separate gated suite''s 11 tests correctly skipped. No code changes needed. Checked off all acceptance-criteria/test checkboxes with a verification note in the description. Leaving task in `doing` for /review (implement does not move to review).'
  timestamp: 2026-07-05T00:27:12.351956+00:00
position_column: done
position_ordinal: 9c80
title: 'Fix HardeningTests README/SECURITY.md drift: ''### Injected globals'' section moved'
---
## What
`Tests/FoundationModelsMultitoolTests/HardeningTests.swift`'s `readmeInjectedGlobalsListMatchesRuntime` test parses a `### Injected globals` section out of the repo root `README.md` (see `HardeningTests.readmeInjectedGlobals()`) and asserts it's set-equal to the runtime-enumerated sandbox globals.

Commit e366c62 ("docs: rewrite README as a library landing page") replaced the M10-hardening-notes README with a library-landing-page README and moved the `## Security model` section (including the `### Injected globals` list) verbatim into `docs/SECURITY.md`, removing it from `README.md`. This broke the machine-checked README<->code sync test: it now fails with `README.md has no "### Injected globals" section.`

Discovered while implementing task 0wf5gad (Add FoundationModelsMetadataRegistry dependency to Package.swift) — confirmed via `git stash -u` that this failure is pre-existing on `main` and unrelated to that task's Package.swift/dependency changes.

## Acceptance Criteria
- [x] `swift test --filter HardeningTests` passes again — either by pointing `HardeningTests.readmeInjectedGlobals()` at `docs/SECURITY.md` (matching where the section actually now lives) or by restoring a `### Injected globals` section to `README.md`, whichever better matches the intended doc structure post-rewrite.
- [x] Full `swift test` is green (no regressions elsewhere).

## Tests
- [x] `swift test --filter HardeningTests`
- [x] Full `swift test`

## Verification note (2026-07-04, /implement pass)
Independently re-verified rather than trusting the prior comment alone. The fix landed on `main` as a side effect of task exbtj1n: `README.md` now has a `## Security model` heading (line 63) with a `### Injected globals` subsection (line 68) listing exactly `console`, `tools`, `help`, `docs`, followed by a link to `docs/SECURITY.md` for full detail — `HardeningTests.readmeInjectedGlobals()` still parses the root README unmodified, so the machine-checked README↔code sync remains anchored there as designed.

Ran fresh (not reused from prior session):
- `swift test --filter HardeningTests` → 17/17 tests passed, including `"README's enumerated 'Injected globals' list is set-equal to the runtime-enumerated sandbox globals"` and `"the injected globals reachable in a fresh MultiTool run... are exactly {console, tools, help, docs}"`.
- Full `swift test` → 250/250 tests passed in 22 suites (exit code 0), plus a separate gated-suite run (11 tests, all skipped as expected/ungated).

No code changes were needed — confirmed fix is real and complete. Task left in `doing` for `/review` per the implement skill's process.
