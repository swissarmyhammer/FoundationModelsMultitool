---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m1hgxqwhddexb04s7twpd83z
  text: |-
    ### finish — interrupted
    - evidence: the first finish sub agent stopped on an API rate limit (session limit) at 2026-09-02 15:57 UTC, during the research step of implement. No source file changed. The task stays in `doing`.
    - next: start the finish loop again on this task.
  timestamp: 2026-09-02T16:58:34.257431+00:00
- actor: claude-code
  id: 01m1hh9vs2rz7bvp1r2v1kbhat
  text: |-
    ### implement — research
    - The text that is wrong stands in `Execute.swift` only: the doc comment at lines 88-89 and the `@Guide` text at line 92. Each other sentence in `Capabilities/Shell/` that names "the current directory of this process" is correct: `ShellCapability.swift:99` and `MultiToolBuilder.swift:233` describe the `nil` default of the parameter, `ShellRunner.swift:167,195,213,443,487,527` describe the last step of the fallback chain, and `SeatbeltSandbox.swift:200` describes `Options()`. No change is necessary in those files.
    - `IntegrationTests/.../Support/ShellRunContextProbe.swift:24` already spells the four labels `withShell(storeDirectory:sandbox:outputChunkStream:defaultWorkingDirectory:)` (done in ^xdwykhb). No change is necessary.
    - The rendered `APISurface` entry carries each `@Guide` text as one `@param args.<name> — <text>` line in `entry.block` (see `Goldens/WeatherTool.ts.txt`), so the surface test can read `entry.block`.
    - `SeatbeltSandboxTests` is the suite that starts the real `sandbox-exec` (the preflight tests). It has no guard for a host with no `sandbox-exec`; each preflight test runs the real wrapper. The new test follows that pattern and adds no guard.
    - Helpers to reuse: `ShellExecuteTests.report(_:)` parses the JSON report, and `ShellExecuteTests.makeVerb(teeing:)` builds a capability and swaps in a private `ProcessRegistry`. The seatbelt scenario needs both, so they move into one shared fixture, `Fixtures/ShellExecuteFixtures.swift`, and `ShellExecuteTests` forwards to it. This removes the copy the review engine would report.
    - The command is `/bin/pwd`, as in `ShellRunnerTests.printedWorkingDirectory`: it prints the physical directory, which is the form `ShellRunner.resolvedDirectory(path:)` gives.
  timestamp: 2026-09-02T17:05:11.458323+00:00
- actor: claude-code
  id: 01m1hhkwgbvj88qxh1qh59qmh9
  text: |-
    ### implement — changed
    - evidence: 4 files — `Sources/FoundationModelsMultitool/Capabilities/Shell/Execute.swift` (the `@Guide` text now reads "Omit it to run in the session's working directory", and the doc comment names the runner default, then the process directory), `Tests/FoundationModelsMultitoolTests/Fixtures/ShellExecuteFixtures.swift` (new: `ShellExecuteVerb.configured(in:)` and `ShellExecuteVerb.report(of:)`, shared by two suites), `Tests/FoundationModelsMultitoolTests/ShellExecuteTests.swift` (two new tests, `executeWithNoWorkingDirectoryRunsInTheCapabilityDefault` and `theExecuteSurfaceNamesTheSessionWorkingDirectory`; the helpers `makeStoreDirectory`, `makeCapability`, `renderedSurface`; `makeVerb(teeing:)` and `report(_:)` forward to the fixture), `Tests/FoundationModelsMultitoolTests/SeatbeltSandboxTests.swift` (one new test, `aPlainCommandUnderASandboxRootedAtTheDefaultIsNotRefused`, with the real `sandbox-exec`).
    - TDD: the first run of the two suites was red with 4 issues (the surface test, and the two `pwd` tests, whose report lines carry a line-number prefix). The source change and the `hasSuffix` read made them green.
    - `swift test --filter "ShellExecuteTests|SeatbeltSandbox"`: 48 tests in 2 suites passed. `swift test --filter "Shell|Sandbox"`: 202 tests in 15 suites passed. `rg "Omit it to run in the current directory" Sources/` finds nothing. The one build warning is the pre-existing SwiftPM note for `mlx-swift_Cmlx.bundle`.
    - The card items that needed no change: `ShellRunContextProbe.swift:24` already spells the four labels; the other "current directory of this process" sentences in `Capabilities/Shell/` are correct statements of the fallback chain; `SeatbeltSandboxTests` has no `sandbox-exec` guard to copy.
    - next: run the whole `swift test`, then commit and review.
  timestamp: 2026-09-02T17:10:39.883417+00:00
depends_on:
- 01M1H2TFSM07W7Z5EQHXDWYKHB
position_column: doing
position_ordinal: '80'
title: Correct the shell working-directory text and prove the Ask 6 scenario
---
## What
Ask 6, part 3 (UPSTREAM_ASKS.md). The text the model reads says "Omit it to run in the current directory", and a model that follows it gets a refused run. Correct the text, and prove the scenario the ask describes end to end.

- `Sources/FoundationModelsMultitool/Capabilities/Shell/Execute.swift`:
  - The `@Guide` text of `ExecuteArguments.workingDirectory` (line 92) becomes "The directory the command runs in. Omit it to run in the session's working directory."
  - The doc comment of `ExecuteArguments.workingDirectory` (line 88-89) names the runner default, then the process directory.
  - The verb `description` (line 785) keeps "workingDirectory to run it somewhere else".
- Search `Sources/FoundationModelsMultitool/Capabilities/Shell/` for every other sentence that states the old fallback ("the current directory of this process", "Omit it to run in the current directory") and correct each one that is now wrong.
- `IntegrationTests/Tests/FoundationModelsMultitoolIntegrationTests/Support/ShellRunContextProbe.swift` line 24 spells the three-argument `withShell` signature; add the fourth label. `ShellBackgroundTests.swift` and `Support/ShellBackgroundRunner.swift` in the same package name `withShell()` with no labels and need no change. Do not change the behavior of the integration tests.
- No golden pins the `tools.shell.execute` rendered text: `Tests/FoundationModelsMultitoolTests/Goldens/` holds `BuilderSurface.ts.txt`, `MCPSurface.ts.txt` and `WeatherTool.ts.txt`, none of which covers the shell. So the `@Guide` change needs no golden update, and the renderer suites are not in the filter below.

## Acceptance Criteria
- [x] A `tools.shell.execute` call that omits `workingDirectory`, run on a bare `ShellCapability(storeDirectory:defaultWorkingDirectory:)`, prints `ShellRunner.resolvedDirectory(path:)` of the default from `pwd`.
- [x] The Ask 6 scenario: `root` is a temporary directory; the sandbox is `SeatbeltSandbox(options: .init(writableRoots: [root]))`; the capability has `defaultWorkingDirectory: root`; the `execute` call has no `workingDirectory` and the command `pwd`. The result carries no correction (no "outside the configured roots" text) and the output is the resolved root. (At e8c91a6 the same call is refused, because the preflight examines the process directory.)
- [x] The rendered `APISurface` entry for `shell.execute` (`registry.surface.entries.first { $0.path == "shell.execute" }`) contains "session's working directory" and does not contain "run in the current directory".
- [x] `grep -rn "Omit it to run in the current directory" Sources/` finds nothing.

## Tests
- [x] Add to `Tests/FoundationModelsMultitoolTests/ShellExecuteTests.swift`: `executeWithNoWorkingDirectoryRunsInTheCapabilityDefault` (bare call, no session; read `pwd` from the report or with `GetLines`), `theExecuteSurfaceNamesTheSessionWorkingDirectory`.
- [x] Add to `Tests/FoundationModelsMultitoolTests/SandboxPreflightTests.swift` (or `SeatbeltSandboxTests.swift`, whichever already spawns `sandbox-exec`): `aPlainCommandUnderASandboxRootedAtTheDefaultIsNotRefused`, the Ask 6 scenario above. Guard it the way the other seatbelt tests are guarded when `sandbox-exec` is not available.
- [x] Run `swift test --filter "Shell|Sandbox"` and expect every test to pass. Then run the whole `swift test` and expect it green.

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass. #upstream-asks #ask-6