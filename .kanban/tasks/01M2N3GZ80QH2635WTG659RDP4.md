---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m2n4djn8qvtcc1ybn2dyqtj7
  text: |-
    ### implement — no-change
    - evidence: `Package.resolved` already pins FoundationModelsRouter at 7e8260785e8e3c9a11280684d36441e69e865931 (commit cf9cb2d), which has `PendingRunEnvelope.detail`. The checkout under `.build/checkouts` was stale at efe0147. `swift package update FoundationModelsRouter` refreshed the checkout; `Package.resolved` did not change. `swift build --build-tests` then completed (68 s).
    - next: the full `swift test` run for card ^5bwy82d proves the second criterion; this card closes with it.
  timestamp: 2026-09-16T12:52:41.256728+00:00
- actor: claude-code
  id: 01m2n5bf81q1gzze0thhw3xmk2
  text: |-
    ### test — green
    - evidence: `swift build --build-tests` completed (68 s); `swift test` — 1461 tests in 115 suites passed, 0 failed, 0 skipped, 0 warnings. Both acceptance criteria are met. No file changed; `Package.resolved` already pinned the right Router revision.
    - next: none; the card closes.
  timestamp: 2026-09-16T13:09:00.801012+00:00
position_column: done
position_ordinal: ffc480
title: Bump the FoundationModelsRouter pin so the test target compiles again
---
## What

`swift test` does not compile at `652a65d`. `Tests/FoundationModelsMultitoolTests/InlineSettleGraceTests.swift` reads `envelope.detail` on a `PendingRunEnvelope`, but `Package.resolved` pins `FoundationModelsRouter` at `efe0147`, and that revision has no `detail` member. The commit `b6a3f97` (runcode: answer a short snippet with its own result) says the change needs the Router change on the router's `pool` branch. The sibling checkout `../FoundationModelsRouter` on `main` at `7e82607` has `public let detail: String?` on `PendingRunEnvelope`.

Update the Router pin in `Package.resolved` to a revision that has `PendingRunEnvelope.detail`, then run `swift test`.

## Acceptance Criteria

- [ ] `swift build --build-tests` compiles the test target.
- [ ] `swift test` passes in full.

## Tests

- [ ] Run `swift test`; expect zero failures. #operation-tools