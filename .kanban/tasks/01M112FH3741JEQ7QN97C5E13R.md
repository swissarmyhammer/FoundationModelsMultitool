---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m12gs5gd7yzah12630r9f4d9
  text: |-
    ### Discoveries before the implementation

    - `MultiTool` holds five `let`s from the registry: `registry`, `hostFunctions`, `liveTools`, `preamble`, `hintSearcher`. `runSnippet` reads all five. The nested `tools.runCode` makes a new `MultiTool` over `registry` for each run.
    - `SearchToolsTool` holds a `MetadataSearcher` actor built from `registry.surface.entries` and a `limit` that defaults to the entry count. `MetadataSearcher` builds its index at `init`. A selection tier (`SelectionConfig`) is a `Sendable` value that captures the librarian.
    - Two overloads that differ in the return type alone make `let mounted = try registry.makeSessionTools(librarian: nil)` ambiguous. The six callers give no type annotation. So the second method gets a different name: `makeSessionToolsAndStaging(librarian:sampleGenerator:)`.
    - `ForkableTool.forked()` returns `self` for `MultiTool`. A copy of the struct copies the reference to the holder, so a fork shares the box with no code.
    - `ReleaseGate` (`Tests/.../Support/ReleaseGate.swift`) and `TestPoll.waitUntil` (`Fixtures/PollFixtures.swift`) give the in-flight test its gate and its poll.
    - The rules dump for `.swift` is 670,000 characters. I did not read it whole. I apply the five rules the finish prompt names.
  timestamp: 2026-08-27T21:07:27.885544+00:00
- actor: claude-code
  id: 01m12ha6n47tpgm09rghpw98kh
  text: |-
    ### Discoveries during the implementation

    - The second method is named `makeSessionToolsAndStaging(librarian:sampleGenerator:)`. An overload of `makeSessionTools` that differs in the return type alone makes the six callers ambiguous; the doc comment of the new method records this.
    - `RegistryBundle` carries a `RegistryBundleShape` (`bindsSearchTools`, `discovery`), so `RegistryHolder.applyStaged()` builds the next bundle the same way as the first. The bundle also holds the discovery searcher of `searchTools` (`DiscoverySearch.configured(selection:)`), because `MetadataSearcher` builds its index at `init` and the selection tier is bound to the librarian. `SearchToolsTool.makeSearcher(over:selection:)`, `makeSelection(librarian:)` and `makeSample(generator:)` are the one place each of these is built; `init(registry:librarian:...)` and `makeSessionToolsAndStaging` both call them.
    - `SearchToolsTool` keeps a `Catalog` enum: `.fixed(searcher:limit:)` for the public `init(searcher:limit:sample:)` that the older suites use, and `.shared(holder:limit:)` for the tool `makeSessionToolsAndStaging` mounts. `call` reads the holder one time at its start.
    - The nested `tools.runCode` gets a `RegistryHolder(current: bundle)` over the bundle of the run that started it, so a nested snippet resolves the same surface as its parent snippet and a swap reaches neither.
    - `applyStaged()` builds the new bundle under the `Mutex`, so a run that starts during the swap reads the whole old bundle or the whole new one.
    - A `GatedTool` fixture with a `ToolReleaseLatch` exists in `Fixtures/SuspendedContextFixtures.swift`. The in-flight test uses it; I removed a second copy I had written.
    - No formatter configuration file is in the repository root, so no formatter ran.
  timestamp: 2026-08-27T21:16:46.116376+00:00
- actor: claude-code
  id: 01m12hadqm2fdgd9yvd3bprakj
  text: |-
    ### implement — changed
    - evidence: 9 files. New: `Sources/FoundationModelsMultitool/RegistryBundle.swift`, `Sources/FoundationModelsMultitool/RegistryHolder.swift`, `Sources/FoundationModelsMultitool/MultiTool+TurnBoundary.swift`, `Tests/FoundationModelsMultitoolTests/RegistrySwapTests.swift`. Changed: `Sources/FoundationModelsMultitool/MultiTool.swift` (holder, `init(holder:...)`, `makeSessionToolsAndStaging`, bundle read at the top of the run), `Sources/FoundationModelsMultitool/Discovery/SearchToolsTool.swift` (`Catalog`, `init(holder:...)`, `makeSearcher`/`makeSelection`/`makeSample`), `Sources/FoundationModelsMultitool/MultiTool+Forking.swift` (doc comment), `Tests/FoundationModelsMultitoolTests/Fixtures/MultiToolExecutionFixtures.swift` (no net change; a copy was added and removed). `swift build --build-tests`: no error, no warning in the changed files. `swift test`: 1277 tests in 96 suites passed, 0 failed. `swift build --package-path IntegrationTests --build-tests`: build complete. All 10 checkboxes on the card are ticked.
    - next: `test` step, then `commit`, then `review`.
  timestamp: 2026-08-27T21:16:53.364238+00:00
depends_on:
- 01M1129XDMAA4E667NF980B1W6
- 01M112EG33CSGN466M9BHVD8C0
position_column: doing
position_ordinal: '80'
title: Swap the Registry in MultiTool at the turn boundary
---
## What
The second half of rebuild-and-swap. eventplan.md § "Consolidation of the siblings": "Then MultiTool swaps it in atomically at the next turn boundary — the same boundary where the outbox folds in events. Nothing changes below a snippet that runs. An in-flight run keeps the registry that it started with."

Router supplies the boundary through `TurnBoundaryTool.turnWillBegin()` (Router task `01M112SF3QTAYXEPRWNW77K41M`; stub `980b1w6` here). This task makes `MultiTool` conform and apply a staged registry there.

- Add `Sources/FoundationModelsMultitool/RegistryBundle.swift`: today `registry`, `hostFunctions`, `liveTools`, `preamble`, and `hintSearcher` are `let`s set in `MultiTool.init`. Group them into one `RegistryBundle` value built from a `Registry` in one function.
- Add `Sources/FoundationModelsMultitool/RegistryHolder.swift`: a `final class RegistryHolder: Sendable` (a reference box; `Mutex` from Synchronization is `~Copyable`, and `MultiTool` is a copyable struct, so the lock lives in the box, not in the struct). It holds the current bundle and the newest staged registry. `stage(_:)` keeps only the newest. `applyStaged()` moves the staged registry into the current bundle.
- Modify `Sources/FoundationModelsMultitool/MultiTool.swift`: `MultiTool` holds one `RegistryHolder` and reads `holder.current` one time at the start of `call(arguments:)`. The run keeps that bundle to its end. `public func stage(_ registry: Registry)` forwards to the holder (non-mutating; the box mutates).
- Add `MultiTool+TurnBoundary.swift`: `extension MultiTool: TurnBoundaryTool` with `turnWillBegin()` → `holder.applyStaged()`.
- **Forks share the holder by identity.** `MultiTool+Forking.swift` takes the blanket `forked()` that returns `self`, and its doc comment argues for that. Keep it. A fork copies the struct, which copies the reference to the same box, thus a fork and its parent swap together at the same tick. Add two sentences to that file's doc comment: the registry now changes only at a turn boundary; a run keeps the one it started with; forks share the box.
- **`SearchToolsTool` swaps with the same box.** It reads `registry.surface.entries` for selection. Give it the holder too, so `tools.searchTools` and the mounted `findAPIs` see the same registry as `runCode` at the same tick.
- **`Registry.makeSessionTools(...)` keeps its signature** (`[any FoundationModels.Tool]`). Its callers are `Sources/MultitoolCLI/CLIRunner.swift:467`, `IntegrationTests/Tests/FoundationModelsMultitoolIntegrationTests/Support/ScenarioRunner.swift:753`, `.../Support/ShellBackgroundRunner.swift:217`, and `Tests/FoundationModelsMultitoolTests/MultiToolExecutionTests.swift:345,359,372`; none of them changes. Add a second method, `makeSessionTools(...) -> (tools: [any Tool], staging: any RegistryStaging)`, where `RegistryStaging` is a small protocol with `stage(_:)`, for the refresher task. The old method calls the new one and drops the second value.
- The parked runs and the mailbox are untouched: the mailbox has session scope in Router.

## Acceptance Criteria
- [x] `stage(_:)` then `turnWillBegin()` makes the next `runCode` see the new verbs, and `help()` / `docs()` / `findAPIs` see them at the same time.
- [x] `stage(_:)` with no `turnWillBegin()` leaves the current surface unchanged.
- [x] A snippet in flight across a swap completes against the registry it started with (start a slow `ScriptedTool` call, stage, tick, and assert the run's later `tools.*` call in the same snippet resolves the old surface).
- [x] Two stages then one tick gives the newest registry.
- [x] A fork made before a stage sees the swap after the tick (shared box).
- [x] The six existing `makeSessionTools` callers compile with no change.
- [x] `swift build` succeeds.

## Tests
- [x] Add `Tests/FoundationModelsMultitoolTests/RegistrySwapTests.swift` with the criteria above, including the fork case (no forking suite exists today; this file holds it).
- [x] `swift test --filter RegistrySwapTests` passes.
- [x] Full `swift test` passes.

## Workflow
- Use `/tdd` — write the swap tests first, then implement. #eventplan #phase-4