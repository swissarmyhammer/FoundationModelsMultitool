{"id":"01M3A3549V97XC8SYM7HZSTBAW","timestamp":"2026-09-24T16:14:38.907263Z","op":"create","item_id":"01M3A3549T6VK6T8H9827SEZQ8","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,27 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01M3A32XTTF1JWYSA9GPDZP3KQ\n+position_column: todo\n+position_ordinal: '9180'\n+title: 'Web: document the web capability in README.md and docs/SECURITY.md'\n+---\n+## What\n+Document the shipped capability. Design: `web.md` § \"Security\" (the five statements), § \"Configuration\", and § \"The provider list\".\n+\n+- `README.md`, section `## Capabilities` (`:45`): add `web` beside files, shell, and MCP. State `withWeb(configuration:)`, the two verbs `tools.web.search` and `tools.web.fetch`, the keyless default, the environment variables of the six keyed providers, and one short snippet (search, then `Promise.all` over `fetch`). Keep the `### Injected globals` list with no change (the web capability adds no global).\n+- `docs/SECURITY.md`: add a section for the web capability that states: it is off by default; each request and each redirect hop goes through `WebAddressGuard`; the DNS rebinding limit and the advice to use a network policy outside the process; keys stay in Swift and the sandbox never sees them; page content is untrusted data that can contain instructions; the SearXNG base URL is host configuration and is not guarded. Keep the statement that the sandbox itself has no network access, and add that network access comes only from a mounted tool.\n+- Write both in ASD-STE100 Simplified Technical English, the same as `web.md`.\n+\n+## Acceptance Criteria\n+- [ ] The README `## Capabilities` section names `withWeb`, `tools.web.search`, `tools.web.fetch`, and each of the environment variable names.\n+- [ ] `docs/SECURITY.md` has a web section with each of the six statements above.\n+- [ ] `HardeningTests` (the injected-globals list) still passes.\n+\n+## Tests\n+- [ ] Create `Tests/FoundationModelsMultitoolTests/WebDocumentationTests.swift` that reads the files with `RepositoryFile.read(relativePath:)` and checks: the README `## Capabilities` section (text up to the next `\\n## `) contains `withWeb`, `tools.web.search`, `tools.web.fetch`, and each variable name; `docs/SECURITY.md` contains a web section that names `WebAddressGuard`, `DNS rebinding`, and `SearXNG`.\n+- [ ] Run `swift test --filter \"WebDocumentationTests|HardeningTests\"`. All pass. Then run `swift test`.\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass. #web\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,27 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01M3A32XTTF1JWYSA9GPDZP3KQ\n-position_column: todo\n-position_ordinal: '9180'\n-title: 'Web: document the web capability in README.md and docs/SECURITY.md'\n----\n-## What\n-Document the shipped capability. Design: `web.md` § \"Security\" (the five statements), § \"Configuration\", and § \"The provider list\".\n-\n-- `README.md`, section `## Capabilities` (`:45`): add `web` beside files, shell, and MCP. State `withWeb(configuration:)`, the two verbs `tools.web.search` and `tools.web.fetch`, the keyless default, the environment variables of the six keyed providers, and one short snippet (search, then `Promise.all` over `fetch`). Keep the `### Injected globals` list with no change (the web capability adds no global).\n-- `docs/SECURITY.md`: add a section for the web capability that states: it is off by default; each request and each redirect hop goes through `WebAddressGuard`; the DNS rebinding limit and the advice to use a network policy outside the process; keys stay in Swift and the sandbox never sees them; page content is untrusted data that can contain instructions; the SearXNG base URL is host configuration and is not guarded. Keep the statement that the sandbox itself has no network access, and add that network access comes only from a mounted tool.\n-- Write both in ASD-STE100 Simplified Technical English, the same as `web.md`.\n-\n-## Acceptance Criteria\n-- [ ] The README `## Capabilities` section names `withWeb`, `tools.web.search`, `tools.web.fetch`, and each of the environment variable names.\n-- [ ] `docs/SECURITY.md` has a web section with each of the six statements above.\n-- [ ] `HardeningTests` (the injected-globals list) still passes.\n-\n-## Tests\n-- [ ] Create `Tests/FoundationModelsMultitoolTests/WebDocumentationTests.swift` that reads the files with `RepositoryFile.read(relativePath:)` and checks: the README `## Capabilities` section (text up to the next `\\n## `) contains `withWeb`, `tools.web.search`, `tools.web.fetch`, and each variable name; `docs/SECURITY.md` contains a web section that names `WebAddressGuard`, `DNS rebinding`, and `SearXNG`.\n-- [ ] Run `swift test --filter \"WebDocumentationTests|HardeningTests\"`. All pass. Then run `swift test`.\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass. #web\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M3A3H16QF68G0HQ7DFVMHT61","timestamp":"2026-09-24T16:21:08.951250Z","op":"update","item_id":"01M3A3549T6VK6T8H9827SEZQ8","forward_patch":"--- original\n+++ modified\n@@ -8,19 +8,28 @@\n title: 'Web: document the web capability in README.md and docs/SECURITY.md'\n ---\n ## What\n-Document the shipped capability. Design: `web.md` § \"Security\" (the five statements), § \"Configuration\", and § \"The provider list\".\n+Document the shipped capability, and bring `web.md` in line with what ships. Design: `web.md` § \"Security\", § \"Configuration\", § \"The provider list\".\n\n-- `README.md`, section `## Capabilities` (`:45`): add `web` beside files, shell, and MCP. State `withWeb(configuration:)`, the two verbs `tools.web.search` and `tools.web.fetch`, the keyless default, the environment variables of the six keyed providers, and one short snippet (search, then `Promise.all` over `fetch`). Keep the `### Injected globals` list with no change (the web capability adds no global).\n-- `docs/SECURITY.md`: add a section for the web capability that states: it is off by default; each request and each redirect hop goes through `WebAddressGuard`; the DNS rebinding limit and the advice to use a network policy outside the process; keys stay in Swift and the sandbox never sees them; page content is untrusted data that can contain instructions; the SearXNG base URL is host configuration and is not guarded. Keep the statement that the sandbox itself has no network access, and add that network access comes only from a mounted tool.\n-- Write both in ASD-STE100 Simplified Technical English, the same as `web.md`.\n+- `README.md`, section `## Capabilities` (`:45`): add `web` beside files, shell, and MCP. State `withWeb(configuration:)`, the two verbs `tools.web.search` and `tools.web.fetch`, the keyless default, the environment variables of the six keyed providers (`BRAVE_SEARCH_API_KEY`, `TAVILY_API_KEY`, `EXA_API_KEY`, `SERPER_API_KEY`, `KAGI_API_KEY`, `SEARXNG_URL`), and one short snippet (search, then `Promise.all` over `fetch`). Keep the `### Injected globals` list with no change (the web capability adds no global).\n+- `docs/SECURITY.md`: add a section `## The web capability` that contains each of these fixed phrases, each in a sentence that explains it:\n+  1. `off by default` and `withWeb`\n+  2. `each redirect hop` and `WebAddressGuard`\n+  3. `DNS rebinding`\n+  4. `Keys stay in Swift`\n+  5. `can contain instructions` (page content is untrusted data)\n+  6. `SearXNG` (the base URL is host configuration and is not guarded; its redirect hops and the result URLs are)\n+  7. `only from a mounted tool` (the sandbox itself has no network access; network access comes only from a mounted tool)\n+- `web.md`: update the § \"Files to add or change\" list and the Level 1 test table to the file and suite names that shipped, if any differ from the names there now.\n+- Write in ASD-STE100 Simplified Technical English, the same as `web.md`.\n\n ## Acceptance Criteria\n-- [ ] The README `## Capabilities` section names `withWeb`, `tools.web.search`, `tools.web.fetch`, and each of the environment variable names.\n-- [ ] `docs/SECURITY.md` has a web section with each of the six statements above.\n+- [ ] The README `## Capabilities` section names `withWeb`, `tools.web.search`, `tools.web.fetch`, and each of the six variable names.\n+- [ ] `docs/SECURITY.md` has the section `## The web capability` with each of the seven fixed phrases.\n - [ ] `HardeningTests` (the injected-globals list) still passes.\n+- [ ] Each suite name in the Level 1 table of `web.md` matches a file under `Tests/FoundationModelsMultitoolTests/`.\n\n ## Tests\n-- [ ] Create `Tests/FoundationModelsMultitoolTests/WebDocumentationTests.swift` that reads the files with `RepositoryFile.read(relativePath:)` and checks: the README `## Capabilities` section (text up to the next `\\n## `) contains `withWeb`, `tools.web.search`, `tools.web.fetch`, and each variable name; `docs/SECURITY.md` contains a web section that names `WebAddressGuard`, `DNS rebinding`, and `SearXNG`.\n+- [ ] Create `Tests/FoundationModelsMultitoolTests/WebDocumentationTests.swift` that reads the files with `RepositoryFile.read(relativePath:)` and checks: the README `## Capabilities` section (text up to the next `\\n## `) contains `withWeb`, `tools.web.search`, `tools.web.fetch`, and each variable name; the `## The web capability` section of `docs/SECURITY.md` contains each of the seven fixed phrases.\n - [ ] Run `swift test --filter \"WebDocumentationTests|HardeningTests\"`. All pass. Then run `swift test`.\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -8,28 +8,19 @@\n title: 'Web: document the web capability in README.md and docs/SECURITY.md'\n ---\n ## What\n-Document the shipped capability, and bring `web.md` in line with what ships. Design: `web.md` § \"Security\", § \"Configuration\", § \"The provider list\".\n+Document the shipped capability. Design: `web.md` § \"Security\" (the five statements), § \"Configuration\", and § \"The provider list\".\n\n-- `README.md`, section `## Capabilities` (`:45`): add `web` beside files, shell, and MCP. State `withWeb(configuration:)`, the two verbs `tools.web.search` and `tools.web.fetch`, the keyless default, the environment variables of the six keyed providers (`BRAVE_SEARCH_API_KEY`, `TAVILY_API_KEY`, `EXA_API_KEY`, `SERPER_API_KEY`, `KAGI_API_KEY`, `SEARXNG_URL`), and one short snippet (search, then `Promise.all` over `fetch`). Keep the `### Injected globals` list with no change (the web capability adds no global).\n-- `docs/SECURITY.md`: add a section `## The web capability` that contains each of these fixed phrases, each in a sentence that explains it:\n-  1. `off by default` and `withWeb`\n-  2. `each redirect hop` and `WebAddressGuard`\n-  3. `DNS rebinding`\n-  4. `Keys stay in Swift`\n-  5. `can contain instructions` (page content is untrusted data)\n-  6. `SearXNG` (the base URL is host configuration and is not guarded; its redirect hops and the result URLs are)\n-  7. `only from a mounted tool` (the sandbox itself has no network access; network access comes only from a mounted tool)\n-- `web.md`: update the § \"Files to add or change\" list and the Level 1 test table to the file and suite names that shipped, if any differ from the names there now.\n-- Write in ASD-STE100 Simplified Technical English, the same as `web.md`.\n+- `README.md`, section `## Capabilities` (`:45`): add `web` beside files, shell, and MCP. State `withWeb(configuration:)`, the two verbs `tools.web.search` and `tools.web.fetch`, the keyless default, the environment variables of the six keyed providers, and one short snippet (search, then `Promise.all` over `fetch`). Keep the `### Injected globals` list with no change (the web capability adds no global).\n+- `docs/SECURITY.md`: add a section for the web capability that states: it is off by default; each request and each redirect hop goes through `WebAddressGuard`; the DNS rebinding limit and the advice to use a network policy outside the process; keys stay in Swift and the sandbox never sees them; page content is untrusted data that can contain instructions; the SearXNG base URL is host configuration and is not guarded. Keep the statement that the sandbox itself has no network access, and add that network access comes only from a mounted tool.\n+- Write both in ASD-STE100 Simplified Technical English, the same as `web.md`.\n\n ## Acceptance Criteria\n-- [ ] The README `## Capabilities` section names `withWeb`, `tools.web.search`, `tools.web.fetch`, and each of the six variable names.\n-- [ ] `docs/SECURITY.md` has the section `## The web capability` with each of the seven fixed phrases.\n+- [ ] The README `## Capabilities` section names `withWeb`, `tools.web.search`, `tools.web.fetch`, and each of the environment variable names.\n+- [ ] `docs/SECURITY.md` has a web section with each of the six statements above.\n - [ ] `HardeningTests` (the injected-globals list) still passes.\n-- [ ] Each suite name in the Level 1 table of `web.md` matches a file under `Tests/FoundationModelsMultitoolTests/`.\n\n ## Tests\n-- [ ] Create `Tests/FoundationModelsMultitoolTests/WebDocumentationTests.swift` that reads the files with `RepositoryFile.read(relativePath:)` and checks: the README `## Capabilities` section (text up to the next `\\n## `) contains `withWeb`, `tools.web.search`, `tools.web.fetch`, and each variable name; the `## The web capability` section of `docs/SECURITY.md` contains each of the seven fixed phrases.\n+- [ ] Create `Tests/FoundationModelsMultitoolTests/WebDocumentationTests.swift` that reads the files with `RepositoryFile.read(relativePath:)` and checks: the README `## Capabilities` section (text up to the next `\\n## `) contains `withWeb`, `tools.web.search`, `tools.web.fetch`, and each variable name; `docs/SECURITY.md` contains a web section that names `WebAddressGuard`, `DNS rebinding`, and `SearXNG`.\n - [ ] Run `swift test --filter \"WebDocumentationTests|HardeningTests\"`. All pass. Then run `swift test`.\n\n ## Workflow\n","transaction_id":null}
