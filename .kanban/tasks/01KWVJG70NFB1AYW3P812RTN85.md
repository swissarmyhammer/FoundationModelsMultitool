---
comments:
- actor: claude-code
  id: 01kwvy4yrn7zf05hp2xy26kbrb
  text: 'Moved to doing. Read task, APISurface.swift, ToolAPIRenderer.swift, ToolDescriptor.swift, FindAPITool.swift, MultiTool.swift''s docs()/help(), and existing tests (FindAPIToolTests, BuilderSurfaceTests, HelpDocsTests). Extended `groupedSelectionSplicesQualifiedPath` in FindAPIToolTests.swift with assertions that feedback contains the qualified `tools.github.createIssue(` call and does not contain the bare `tools.createIssue(` call. Confirmed RED: `swift test --filter FindAPIToolTests` fails at both new assertions for the expected reason (embedded @example and trailer both still say bare `tools.createIssue(`). Proceeding to implement the Entry-level qualification substitution.'
  timestamp: 2026-07-06T14:43:37.365860+00:00
- actor: claude-code
  id: 01kwvyb7v7xjzb0ad6ke3bhh6j
  text: |-
    Implementation landed:
    - APISurface.swift: added private `Entry.qualify(_:)` helper that substitutes the bare `tools.<name>(` call prefix for the fully-qualified `tools.<path>(` prefix; `block` now applies it to `descriptor.source` (so the embedded JSDoc `@example` line is qualified); added public `Entry.qualifiedExample` applying it to `descriptor.example`.
    - FindAPITool.swift: `format`'s "Example: ..." trailer now uses `match.item.qualifiedExample` instead of the raw `descriptor.example`, so the trailer and `block`'s embedded example never disagree.
    - FindAPIToolTests.swift: extended `groupedSelectionSplicesQualifiedPath` with assertions for the qualified `tools.github.createIssue(` call and absence of the bare `tools.createIssue(` — confirmed RED before the fix, GREEN after.
    - Goldens/BuilderSurface.ts.txt: updated grouped entries' `@example` lines to the qualified form.

    Verification: `swift build` and full `swift test` both green (250 tests passed, 11 GPU-gated tests skipped as expected, same as baseline). Targeted runs of FindAPIToolTests, BuilderSurfaceTests, HelpDocsTests all green individually too.

    Spawned the double-check adversarial review agent per really-done; awaiting its verdict before final handoff.
  timestamp: 2026-07-06T14:47:03.271836+00:00
- actor: claude-code
  id: 01kwvyjvwccas6cb3511fjjqhd
  text: |-
    double-check verdict: REVISE, with two findings.

    Finding 1 (medium): `Entry.qualify(_:)`'s doc comment overclaimed that "tools.<name>(" can't appear anywhere in descriptor's rendered text except as the intended call — actually a tool author's free-form description/@Guide prose (also embedded verbatim in descriptor.source) could in principle contain that exact literal substring, which `replacingOccurrences` would then also rewrite. This is the same blanket-substitution-over-descriptor.source approach the task description itself explicitly prescribes ("applied everywhere that prefix appears in descriptor.source/block... this prefix is unambiguous and safe to target"), and `ToolAPIRenderer.render` only ever emits that literal substring at the @example/example call site itself, so this is a theoretical, not practical, risk for any real generated tool doc. Rather than expand scope into restructuring ToolAPIRenderer/ToolDescriptor to expose the example line separately (out of scope for this task, which directed an Entry-level substitution), I corrected the doc comment's safety claim to state the accurate, narrower guarantee (splice-safety, not search-substring-uniqueness) and note the theoretical edge case explicitly. Logged here as the justification for proceeding without a broader refactor.

    Finding 2 (low, doc staleness): APISurface+SearchableMetadata.swift's renderBlock() doc comment said `block` splices descriptor.source "verbatim" — no longer true post-fix. Fixed: now says descriptor.source "with its embedded @example call qualified to the entry's fully-qualified path".

    Re-ran swift build and full swift test after both doc fixes: build exit 0, 250 tests passed, 11 GPU-gated tests skipped as expected (unchanged from baseline). No functional code changed in this follow-up, only doc comments.

    Task is really-done. Leaving in `doing` for /review per the implement skill's process (not moving to review myself).
  timestamp: 2026-07-06T14:51:13.164165+00:00
position_column: doing
position_ordinal: '80'
title: Qualify grouped tools' rendered example call path in findAPIs/docs()/help() results
---
## What\nFor a grouped tool (added via `MultiTool.Builder.addGroup(named:_:)`), the runnable example text the model actually sees is **wrong** — it shows the bare, unqualified call (`tools.search(...)`) instead of the real, fully-qualified call path (`tools.github.search(...)`) needed to invoke it. The correct qualified path currently appears *only* as a `//` comment banner immediately above the (wrong) example — and a model cannot be expected to infer \"prepend the group name from a nearby comment\" on its own. Per user: \"the model is never gonna figure to do tools.github... what I expected was a top level search function that searches all tools and tells you enough in the search results to be properly qualified.\" The fix belongs in what the search results/docs *say*, not in the model's inference.\n\nRoot cause: `ToolAPIRenderer.render` (`Sources/FoundationModelsMultitool/Surface/ToolAPIRenderer.swift`) renders each tool's `ToolDescriptor` — `declaration`/`doc`/`example`/`source` — namespace-agnostically (deliberately: one render call per tool, reused for the runtime binding regardless of where it ends up namespaced). `APISurface.Entry` (`Sources/FoundationModelsMultitool/Surface/APISurface.swift`) layers the real, fully-qualified `path` (`\"<group>.<name>\"` for a grouped tool) on top, but only as a `// tools.<path>` banner line prepended to `descriptor.source` — `descriptor.source`'s own embedded JSDoc `@example` line, and the standalone `descriptor.example` field, both still read `tools.<name>(...)` (bare), since they were rendered before the entry's namespace was known. This mismatch is visible today in `Tests/FoundationModelsMultitoolTests/Goldens/BuilderSurface.ts.txt`: the `github.createIssue`/`github.search` entries' banners correctly say `// tools.github.createIssue` / `// tools.github.search`, but their `@example` lines say `tools.createIssue(...)` / `tools.search(...)`.\n\nThis affects every consumer of `Entry.block`/`APISurface.source`, not just `findAPIs`:\n- `FindAPITool.format` (`Sources/FoundationModelsMultitool/Agent/FindAPITool.swift`) splices `match.item.block` *and* separately appends `\"Example: \\(match.item.descriptor.example)\"` — both wrong for a grouped tool.\n- `MultiTool.swift`'s `docs(name)` returns `entry.block` directly — same embedded wrong `@example`.\n- `APISurface.source` (every entry's `.block`, joined) backs the registry-backed selection tier's own instruction prefix — so even the *selection* model reads the wrong example when deciding what's relevant.\n\nFix `APISurface.Entry` so its rendered text always shows the fully-qualified call path in every place an example/call is shown — not just the banner. Since `descriptor.example`/the embedded `@example` line are always generated as exactly `\"tools.\\(descriptor.name)(...)\"` (per `ToolAPIRenderer.render`'s `exampleCall` construction — `descriptor.name` is validated as a legal TS identifier, so this prefix is unambiguous and safe to target), the qualification can be done at the `Entry` level with a targeted substitution of the `\"tools.\\(descriptor.name)(\"` prefix for `\"tools.\\(path)(\"` — a no-op for a standalone entry (`path == descriptor.name`), and correctly qualifying it for a grouped entry — applied everywhere that prefix appears in `descriptor.source`/`block` (the embedded JSDoc `@example`) and in `descriptor.example` itself (used by `FindAPITool.format`'s separate trailer line).\n\n## Acceptance Criteria\n- [x] `APISurface.Entry.block` (and therefore `APISurface.source`, `docs(name)`'s result, and the registry-backed selection tier's instruction prefix) shows the fully-qualified `tools.<path>(...)` call in its embedded `@example` line for a grouped tool — never the bare `tools.<name>(...)`.\n- [x] `FindAPITool.format`'s separate `\"Example: ...\"` trailer also shows the qualified call for a grouped tool (either by deriving it the same way, or by removing the now-redundant trailer if `block`'s own embedded example already suffices — pick whichever avoids showing two different, possibly-conflicting example strings in the same feedback text).\n- [x] A standalone tool's rendered example is unaffected (`path == descriptor.name`, so the substitution is a no-op) — no behavior change for the non-grouped case.\n- [x] `swift build` and full `swift test` remain green.\n\n## Tests\n- [x] `Tests/FoundationModelsMultitoolTests/FindAPIToolTests.swift`'s `groupedSelectionSplicesQualifiedPath` — currently only asserts the `// tools.github.createIssue` banner is present; add an assertion that the feedback also contains the qualified example call `tools.github.createIssue(` and does **not** contain the bare, wrong `tools.createIssue(` as a call (as opposed to substring-matching inside the correct qualified form).\n- [x] `Tests/FoundationModelsMultitoolTests/Goldens/BuilderSurface.ts.txt` — update the golden text so `github.createIssue`/`github.search`'s `@example` lines show `tools.github.createIssue(...)`/`tools.github.search(...)`; the existing `BuilderSurfaceTests.fixtureSetMatchesGoldenFile` test must still pass against the updated golden.\n- [x] `swift test --filter FindAPIToolTests` and `swift test --filter BuilderSurfaceTests` pass.\n- [x] Full `swift test` passes with no regressions (check `HelpDocsTests`/`docs(name)`-related tests too, since they also read `entry.block`).\n\n## Workflow\n- Use `/tdd` — first extend `groupedSelectionSplicesQualifiedPath` (or add a new test) asserting the qualified example, watch it fail against the current unqualified output, then implement the `Entry`-level qualification to make it pass, then update the `BuilderSurface.ts.txt` golden to match.\n