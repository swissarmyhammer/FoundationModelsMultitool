---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m2nry72t84xk76a3fbjhx2gs
  text: |-
    ### implement — research

    - Splice sites that put schema text between double quotes with `escapeForJSStringLiteral` only: `objectKeyLiteral(_:)` (used by `render(name:description:arguments:)` example fields, `declaredType(ofObject:)` and `exampleObjectLiteral` in both paths), `tsLiteral(_:)` `.string` case (used by `enumUnion` for the declaration and the `@param` one-of clause, and by both `exampleLiteral` synthesizers for the first choice), and the `.string` placeholder in `exampleLiteral(for:name:context:)` and `exampleLiteral(for:name:)`.
    - `jsStringLiteral(_:)` already escapes the backslash, the quote, LF, CR, U+2028 and U+2029, and `jsStringLiteralEscapesEveryQuoteBackslashAndLineTerminator` proves it with the `hostileText` / `hostileLiteral` constants. The new tests can reuse those two constants as a property name and as an enum choice.
    - `JSCInterpreter.checkSyntax(of:)` parses a snippet body inside an async function, so `await tools.tool({ ... });` parses when the string literals are correct. The TS `declare function` line is not JavaScript, so the declaration is proved by text assertions only.
    - Callers of `escapeForJSStringLiteral` outside the renderer: none. `TypedMockDryRun` and the test fixtures use `jsStringLiteral`. Its doc comment says `TypedMockDryRun` builds on it, which is no longer true.
    - Not in this card: `paramLine` puts the property name into `@param args.<name>` through `escapeForJSDocComment` only. A raw LF in a name makes a doc line without the ` * ` prefix. The block still parses. Recorded here as a possible follow-up.
  timestamp: 2026-09-16T18:51:17.978105+00:00
- actor: claude-code
  id: 01m2ns5fcze9h80esme96pqzc9
  text: |-
    ### implement — implementation landed

    RED: three tests in `ToolAPIRendererTests` use the `hostileText` / `hostileLiteral` constants (a quote, a backslash, LF, CR, U+2028, U+2029) as a property name and as an enum choice. Before the fix, `swift test --filter "ToolAPIRendererTests/(propertyNameWithLineTerminators|enumChoiceWithLineTerminators|stringPlaceholderWithLineTerminators)"` failed with 25 issues: raw terminators in the declaration and the example, `checkSyntax` threw `Unexpected EOF (line 1)`, and on the schema path the `JavaScriptLiteralSyntax` guard refused the synthesized literal.

    GREEN, in `Sources/FoundationModelsMultitool/Surface/ToolAPIRenderer.swift`:
    - `objectKeyLiteral(_:)` returns `jsStringLiteral(key)` for a key that is not a bare identifier.
    - `tsLiteral(_:)` `.string` returns `jsStringLiteral(string)`.
    - The `.string` placeholder in `exampleLiteral(for:name:context:)` and in `exampleLiteral(for:name:)` returns `jsStringLiteral(name)`.
    - `escapeForJSStringLiteral(_:)` is now `private`. Its only caller is `jsStringLiteral`. Its doc said `TypedMockDryRun` builds on it; that was not true, `TypedMockDryRun` and the fixtures use `jsStringLiteral`. Tests do not call it. The doc comments of the four functions now say which one does the escaping.

    Proof: each test asserts the escaped literal in the declaration, the `@param` one-of clause and the example, asserts no raw terminator in the declaration and the example, and parses the example with `JSCInterpreter().checkSyntax(of:)`. The placeholder test renders the same name through the typed path and through a schema built with `JSONSerialization`, and both examples are equal.

    Goldens: `git status` shows no change under `Tests/FoundationModelsMultitoolTests/Goldens`, and the golden-pinned tests in `ToolAPIRendererTests` and `BuilderSurfaceTests` pass.
  timestamp: 2026-09-16T18:55:15.871483+00:00
- actor: claude-code
  id: 01m2ns5pdn08vg1an04h6ahepy
  text: |-
    ### implement — changed
    - evidence: 2 files — Sources/FoundationModelsMultitool/Surface/ToolAPIRenderer.swift, Tests/FoundationModelsMultitoolTests/ToolAPIRendererTests.swift. `swift test --filter "ToolAPIRendererTests|BuilderSurfaceTests|JavaScriptLiteralSyntaxTests"`: 57 tests in 3 suites passed. `swift test`: 1511 tests in 121 suites passed, 0 failures, 0 skipped, no compiler warning. Goldens unchanged.
    - next: /review
  timestamp: 2026-09-16T18:55:23.061733+00:00
- actor: claude-code
  id: 01m2nsvfp76nhmskpd0691eyc3
  text: |-
    ### review — findings
    - evidence: 1 finding — Tests/FoundationModelsMultitoolTests/ToolAPIRendererTests.swift:585 (swift/fluent-usage). Scope: review sha HEAD~2..HEAD, local backend, iteration 1.
    - next: Add an external label to the second parameter of `expectNoRawLineTerminator`, then check the item and run the review again. The card stays in `review`.
  timestamp: 2026-09-16T19:07:17.063297+00:00
- actor: claude-code
  id: 01m2nsw5hpcmwxxyby8zaafdfd
  text: |-
    ### finish iteration 1 — findings
    - implement: changed — 2 files (Surface/ToolAPIRenderer.swift; ToolAPIRendererTests.swift)
    - test: green — swift test, 1511 tests in 121 suites passed, 0 failed, 0 skipped
    - commit: 8cdc78d (code), 2b26a56 (kanban)
    - review: findings — Tests/FoundationModelsMultitoolTests/ToolAPIRendererTests.swift:585 (swift/fluent-usage: second parameter of `expectNoRawLineTerminator` has no external label)
  timestamp: 2026-09-16T19:07:39.446915+00:00
- actor: claude-code
  id: 01m2nt0m2ztqv559mdqf5f4x9n
  text: |-
    ### implement — review finding worked

    - Finding, word for word: `Tests/FoundationModelsMultitoolTests/ToolAPIRendererTests.swift:585` `swift/fluent-usage` — The second parameter lacks an external label, making the call site less fluent. When parameters form a grammatical phrase, all parameters should be labeled to read clearly at the call site. Add an external label to the second parameter: change `_ label: String` to `labeledAs label: String` (or `description label: String`). This makes the call phrase grammatical: `expectNoRawLineTerminator(in: text, labeledAs: "declaration")`.
    - Fix: `expectNoRawLineTerminator(in:_:)` is now `expectNoRawLineTerminator(in:labeledAs:)`. All five call sites in the file pass `labeledAs:`. The doc comment keeps the `label` parameter entry, because the internal name did not change.
    - Same rule on the other new helpers in the file: `stringPropertySchema(named:)` already has a labeled parameter, and `rawLineTerminators` is a constant. No other change.
    - Note for the next agent: the `files` tool `edit file` with `replace_all: true` changed only the first occurrence in this file. A grep after each edit found the two call sites it missed. Grep after a `replace_all` edit before you trust it.
    - TDD: this step changes a test helper signature only. No production code changed, so there is no RED test to write. The compiler is the check: a call site without the label does not compile after the signature change.
    - The one warning in the `swift test` output, `missing creator for mutated node ... mlx-swift_Cmlx.bundle`, comes from the build system for the `mlx-swift` bundle. It is not from the Swift sources and was present before this change.
  timestamp: 2026-09-16T19:10:05.407606+00:00
- actor: claude-code
  id: 01m2nt0vccvadh4x0kd5r8rw95
  text: |-
    ### implement — changed
    - evidence: 1 file — Tests/FoundationModelsMultitoolTests/ToolAPIRendererTests.swift (the helper signature and five call sites). `swift test --filter ToolAPIRendererTests`: 44 tests in 1 suite passed. `swift test`: 1511 tests in 121 suites passed, 0 failures, 0 skipped, no compiler warning. The review finding item is checked. Package.swift and Package.resolved not touched. Not committed.
    - next: /review
  timestamp: 2026-09-16T19:10:12.876302+00:00
- actor: claude-code
  id: 01m2ntaf6zjxnkx466j2dxfb33
  text: |-
    ### review — clean
    - evidence: `review sha HEAD~2..HEAD` (local backend), zero findings. 1 file reviewed: Tests/FoundationModelsMultitoolTests/ToolAPIRendererTests.swift. 7 validator runs, 0 failed. The one prior finding (swift/fluent-usage, parameter label) is checked.
    - next: none. The card moved review → done.
  timestamp: 2026-09-16T19:15:28.095285+00:00
- actor: claude-code
  id: 01m2ntay61gpthyj8cx8sv9szy
  text: |-
    ### finish iteration 2 — clean
    - implement: changed — 1 file (ToolAPIRendererTests.swift: `labeledAs:` label on the helper and its five call sites)
    - test: green — swift test, 1511 tests in 121 suites passed, 0 failed, 0 skipped
    - commit: d0fc53d (tests), ec92549 (kanban)
    - review: clean — zero findings; card moved to done
  timestamp: 2026-09-16T19:15:43.425657+00:00
position_column: done
position_ordinal: ffcd80
title: A property name or an enum choice with a raw line terminator makes the rendered example and declaration unparsable
---
## What

In `Sources/FoundationModelsMultitool/Surface/ToolAPIRenderer.swift`, three example and declaration builders put schema-derived text between double quotes with `escapeForJSStringLiteral(_:)`, which escapes a backslash and a double quote only:

- `objectKeyLiteral(_:)` for a property name that is not a bare identifier,
- `tsLiteral(_:)` for a string enum choice,
- the `.string` placeholder in `exampleLiteral(for:name:context:)` and `exampleLiteral(for:name:)`.

A raw line feed, carriage return, U+2028 or U+2029 in that text stays raw inside the quotes. JavaScript refuses a raw line feed or carriage return inside a string literal, so the `@example` call and the `declare function` object type do not parse. A `@Generable` struct cannot produce such a name, but a hand-built `DynamicGenerationSchema` or a typed `RenderedParameter` can.

`jsStringLiteral(_:)` in the same file already escapes all four line terminators.

## Changes

1. Route the three builders through `jsStringLiteral(_:)`.
2. Add one test per builder in `Tests/FoundationModelsMultitoolTests/ToolAPIRendererTests.swift` with a name or a choice that holds a raw line feed. Assert the rendered text holds the escape `\\n` and no raw line feed.
3. Keep every golden in `Tests/FoundationModelsMultitoolTests/Goldens` unchanged.

## Acceptance Criteria

- [x] A property name with a raw line feed renders as a quoted key with `\\n` in the declaration and in the example.
- [x] A string enum choice with a raw line feed renders with `\\n` in the declaration, the `@param` clause and the example.
- [x] `swift test --filter ToolAPIRendererTests` passes.

## Workflow
- Use `/tdd`: write the failing tests first, then implement to make them pass. #operation-tools

## Review Findings (2026-09-16 13:58)

> Scope: `review sha HEAD~2..HEAD` — reviewed the diffs only — lines this change added or modified. 2 file(s) reviewed, 4 not reviewed.

> 4 file(s) not reviewed — excluded by an ignore rule:
> - `.kanban/ (from .reviewignore)` — 4 file(s)

- [x] `Tests/FoundationModelsMultitoolTests/ToolAPIRendererTests.swift:585` `swift/fluent-usage` — The second parameter lacks an external label, making the call site less fluent. When parameters form a grammatical phrase, all parameters should be labeled to read clearly at the call site. Add an external label to the second parameter: change `_ label: String` to `labeledAs label: String` (or `description label: String`). This makes the call phrase grammatical: `expectNoRawLineTerminator(in: text, labeledAs: "declaration")`.
