---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m1yngpk44h9f7g1rgxznrfva
  text: |-
    Picked up. Research against the vendored router source at `.build/checkouts/FoundationModelsRouter/Sources/FoundationModelsRouter/`:

    - `ResidencyHold.swift` — one hold per residency, handed to all three `RoutedModel` handles. Its `deinit` calls `router.enqueuePendingRelease(token)`, and the doc comment says "Every resolve drains that queue before it measures the host budget".
    - `LanguageModelProfile.swift` — the class doc says "Residency is pooled. The `Router` reference-counts each resident model across profiles... There is no explicit release: residency ends when the last reference goes away." The profile holds its three handles strongly and needs no hold of its own.
    - The source carries no `release()` member. The card text is correct.

    Discovery outside the card scope: `plan.md` says "one profile resident at a time" in two more places that the card does not name — the retired-passage summary near the end, and the live-suite paragraph. Residency is now pooled, so those sentences are also stale. They do not name `release()`, so they pass the acceptance criteria. Recorded as new work instead of widening this card.

    `dump validators` for `.md` returns zero rules.
  timestamp: 2026-09-07T19:28:57.444025+00:00
- actor: claude-code
  id: 01m1ynk650p45ayvpwqms982fe
  text: |-
    Edited the two named places in the Router surface summary of `plan.md`. The `Router` bullet now says residency is pooled and owned by ARC, and that each `resolve` drains the models that wait for eviction, under the pool lock, before it measures the host budget. The `LanguageModelProfile` bullet now says the three handles share one `ResidencyHold`, that a tool that keeps only `profile.flash` keeps that model resident, and that no call evicts them. The same bullet said "one resident profile" in its next sentence; that is now "one resolved profile", because pooled residency makes the old word wrong.

    `grep -n "release()" plan.md` finds nothing.

    Follow-up card for the three remaining single-residency claims that this card does not name: `^8e14a7`.

    Runs: `swift build` and `swift test`, both clean — 1401 tests in 109 suites passed, zero failures. The one warning in the output ("missing creator for mutated node" for the mlx-swift bundle) comes from the build system and is not from this change. The integration package was not run: this change touches only `plan.md`, which carries no executable content.

    ### implement — changed
    - evidence: 1 file — /Users/wballard/github/swissarmyhammer/FoundationModelsMultitool/plan.md
    - next: /review
  timestamp: 2026-09-07T19:30:18.912884+00:00
position_column: doing
position_ordinal: '80'
title: plan.md still teaches release() as the way to evict a resolved profile
---
## What

Router now owns pooled residency by ARC. `LanguageModelProfile.release()` is deleted (router `^m8vj0jr`, in `main` at `efe0147`), a resident model is freed when the last reference to it goes away, and `Router.resolve` drains the pending evictions before it measures the host budget. Card `^wbya1jz` took the two `profile.release()` call sites out of this repo's Swift code.

`plan.md` still teaches the deleted call. Two places, in the Router surface summary:

- `plan.md:340-341` — "One profile is resident at a time (RAM budget); release before resolving another."
- `plan.md:347` — "``LanguageModelProfile`` — the resolved handle set: `.standard` and `.flash` are `RoutedLLM`, `.embedding` is a `RoutedEmbedder`; `release()` evicts them."

Both sentences name an API that no longer exists, so a reader who follows them writes code that does not compile.

What the text should say instead, confirmed against the router source at `efe0147`:

- `ResidencyHold` is the ARC owner. Each of the three handles holds it strongly, so a tool that keeps only `profile.flash` keeps that model resident, and a live session keeps the profile reachable.
- Dropping the last reference frees the models. There is nothing to call.
- The next `Router.resolve` drains the pending evictions under its pool lock before it measures the host budget, so the next resolve prices against the freed bytes.

## Acceptance Criteria

- [x] `plan.md` names no `release()` on `LanguageModelProfile`.
- [x] `plan.md` states that residency is ARC-owned, and that the next `resolve` drains before it measures.
- [x] No Swift code changes. This card is documentation only.

## Tests

- [x] None. `plan.md` carries no executable content, so `swift test` is unaffected. #router #tech-debt #docs