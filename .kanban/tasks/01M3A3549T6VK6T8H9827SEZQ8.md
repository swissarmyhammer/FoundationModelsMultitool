---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m3cyt228gsatjpz4rv4san4t
  text: |-
    Research done.
    - `withWeb` is in `Surface/MultiToolBuilder+Capabilities.swift`, not in `Surface/MultiToolBuilder.swift` as `web.md` says. The default configuration is `.fromEnvironment()`.
    - More source files shipped than `web.md` lists: `HostResolver.swift`, `BlockedAddresses.swift`, `IPAddress.swift`, `Providers/SearchProviderSupport.swift`.
    - More Level 1 suites shipped: `WebContextTests`, `SearchFreshnessValuesTests`, `KeyedProviderRequestTests`, `SerperKagiRequestTests`, `SearXNGRequestTests`, `SearXNGChainTests`, and the `--web` tests in `CLIArgumentTests+Web.swift`. Each of the 15 suites that the table names already has a file.
    - `WebIntegrationTests/` and `.github/workflows/web.yml` do not exist yet. Other tasks own them, so the list keeps them as planned files.
    - `HardeningTests` reads only the `### Injected globals` list, from that heading to the next heading. Put the web text before that heading.
    - The provider table in `web.md` agrees with the adapters (Kagi is `POST https://kagi.com/api/v1/search` with `Authorization: Bearer`).
  timestamp: 2026-09-25T18:56:25.160003+00:00
- actor: claude-code
  id: 01m3cz4713yfw0x74xm8aaqrg8
  text: |-
    Implementation is done. The work used TDD.
    - RED 1: `RepositoryFileTests` got two tests for a new `RepositoryFile.section(headed:inRelativeFile:)`. The build failed because the member did not exist. GREEN: I added the helper and the error case `RepositoryFileError.sectionNotFound`. `ReadmeOperationSectionTests.section()` now calls the helper, so the section read has one implementation and no copy in the new suite.
    - RED 2: `WebDocumentationTests` has 18 cases (9 README texts and 9 SECURITY phrases). All 18 failed before the document edits.
    - GREEN 2: `README.md` `## Capabilities` has a new `### Web` subsection before `### Injected globals`: `withWeb(configuration:sessionConfiguration:)`, the two verbs, the snippet, the keyless default, a provider and variable table, and "the last call wins". The `### Injected globals` list did not change. `## The demo CLI` names `--web`. `docs/SECURITY.md` has a new `## The web capability` section with the seven fixed phrases.
    - `web.md`: I updated the status note (the code has shipped), the builder file (`Surface/MultiToolBuilder+Capabilities.swift`), the provider note (the adapters were checked against the documentation, and Kagi uses the documented v1 API), and the file list (`HostResolver.swift`, `BlockedAddresses.swift`, `IPAddress.swift`, `Providers/SearchProviderSupport.swift`). The Level 1 table has 8 new rows for the suites that shipped: `WebContextTests`, `KeyedProviderRequestTests`, `SerperKagiRequestTests`, `SearXNGRequestTests`, `SearXNGChainTests`, `SearchFreshnessValuesTests`, `CLIArgumentTests` (+Web), and `WebDocumentationTests`. A script confirmed that each of the 23 suite names has a file.
    - The list keeps `WebIntegrationTests/` and `.github/workflows/web.yml` as planned files, because they do not exist yet.
    - `swift test --filter "WebDocumentationTests|HardeningTests|RepositoryFileTests|ReadmeOperationSectionTests"` gave 32 of 32 passed. `swift test` gave 1790 tests in 143 suites passed. The only warning line is the known `missing creator for mutated node` build-graph notice of the `mlx-swift` bundle. It is not from a changed file.

    ### implement — changed
    - evidence: 7 files — README.md, docs/SECURITY.md, web.md, Tests/FoundationModelsMultitoolTests/WebDocumentationTests.swift (new), Tests/FoundationModelsMultitoolTests/RepositoryFile.swift, Tests/FoundationModelsMultitoolTests/RepositoryFileTests.swift, Tests/FoundationModelsMultitoolTests/ReadmeOperationSectionTests.swift; swift test: 1790 tests in 143 suites passed
    - next: /review
  timestamp: 2026-09-25T19:01:57.923990+00:00
- actor: claude-code
  id: 01m3czatfbdap31a9fpdkkgvbp
  text: |-
    ### test — green
    - evidence: `swift package clean` then `swift test` — 1790 tests, 143 suites, 0 failed, 0 skipped. 0 warnings in this repository's Sources or Tests. 5 warnings found only in vendored `.build/checkouts/mlx-swift` C++/Metal code (constexpr-if C++17 extension notices and one build-system node-creator notice) — out of scope per instructions.
    - next: none. The build is clean.
  timestamp: 2026-09-25T19:05:34.443013+00:00
depends_on:
- 01M3A32XTTF1JWYSA9GPDZP3KQ
position_column: doing
position_ordinal: '80'
title: 'Web: document the web capability in README.md and docs/SECURITY.md'
---
## What
Document the shipped capability, and bring `web.md` in line with what ships. Design: `web.md` § "Security", § "Configuration", § "The provider list".

- `README.md`, section `## Capabilities` (`:45`): add `web` beside files, shell, and MCP. State `withWeb(configuration:)`, the two verbs `tools.web.search` and `tools.web.fetch`, the keyless default, the environment variables of the six keyed providers (`BRAVE_SEARCH_API_KEY`, `TAVILY_API_KEY`, `EXA_API_KEY`, `SERPER_API_KEY`, `KAGI_API_KEY`, `SEARXNG_URL`), and one short snippet (search, then `Promise.all` over `fetch`). Keep the `### Injected globals` list with no change (the web capability adds no global).
- `docs/SECURITY.md`: add a section `## The web capability` that contains each of these fixed phrases, each in a sentence that explains it:
  1. `off by default` and `withWeb`
  2. `each redirect hop` and `WebAddressGuard`
  3. `DNS rebinding`
  4. `Keys stay in Swift`
  5. `can contain instructions` (page content is untrusted data)
  6. `SearXNG` (the base URL is host configuration and is not guarded; its redirect hops and the result URLs are)
  7. `only from a mounted tool` (the sandbox itself has no network access; network access comes only from a mounted tool)
- `web.md`: update the § "Files to add or change" list and the Level 1 test table to the file and suite names that shipped, if any differ from the names there now.
- Write in ASD-STE100 Simplified Technical English, the same as `web.md`.

## Acceptance Criteria
- [x] The README `## Capabilities` section names `withWeb`, `tools.web.search`, `tools.web.fetch`, and each of the six variable names.
- [x] `docs/SECURITY.md` has the section `## The web capability` with each of the seven fixed phrases.
- [x] `HardeningTests` (the injected-globals list) still passes.
- [x] Each suite name in the Level 1 table of `web.md` matches a file under `Tests/FoundationModelsMultitoolTests/`.

## Tests
- [x] Create `Tests/FoundationModelsMultitoolTests/WebDocumentationTests.swift` that reads the files with `RepositoryFile.read(relativePath:)` and checks: the README `## Capabilities` section (text up to the next `\n## `) contains `withWeb`, `tools.web.search`, `tools.web.fetch`, and each variable name; the `## The web capability` section of `docs/SECURITY.md` contains each of the seven fixed phrases.
- [x] Run `swift test --filter "WebDocumentationTests|HardeningTests"`. All pass. Then run `swift test`.

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass. #web