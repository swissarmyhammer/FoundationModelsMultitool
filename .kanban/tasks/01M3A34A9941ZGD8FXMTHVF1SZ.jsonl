{"id":"01M3A34A9AWDHFMAF3F3BFW9Q4","timestamp":"2026-09-24T16:14:12.266358Z","op":"create","item_id":"01M3A34A9941ZGD8FXMTHVF1SZ","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,30 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01M3A33HKP5H238CS992MAJYVS\n+- 01M3A33SMXZ93N45S1SWCZ6NMR\n+position_column: todo\n+position_ordinal: '8e80'\n+title: 'Web: add the web-integration CI job, the build step, and the daily schedule'\n+---\n+## What\n+Run the web live package in CI. Design: `web.md` § \"Testing / CI\".\n+\n+- `.github/workflows/ci.yml`: keep the `ci` job (the call to the shared `swift-ci.yaml`) with no change. Add a job `web-integration` with `needs: ci`, on the same macOS runner class the shared workflow uses. Steps: checkout; `swift build --package-path WebIntegrationTests --build-tests`; `swift test --package-path WebIntegrationTests --no-parallel`. Map the repository secrets `BRAVE_SEARCH_API_KEY`, `TAVILY_API_KEY`, `EXA_API_KEY`, `SERPER_API_KEY`, `KAGI_API_KEY` to `env`, and set `MULTITOOL_WEB_EXPECTED_PROVIDERS` to the list of providers whose secrets the repository has.\n+- Add a `schedule:` trigger (daily, for example `cron: \"17 6 * * *\"`) so markup drift of the keyless providers shows before a user finds it. The `ci` job can keep running on the schedule too; if that is too expensive, put the web job in a new file `.github/workflows/web.yml` with the schedule and a `workflow_run` or `push` trigger, and pin that file instead.\n+- The build of `WebIntegrationTests` on every run keeps a broken live test visible, the same reason as `IntegrationTests/Package.swift:58-66`. If the shared workflow can take a second package path, prefer that input; else the build step in the new job is sufficient.\n+- Update the header comment of `ci.yml` to name the second package and why it reads secrets.\n+\n+## Acceptance Criteria\n+- [ ] The workflow file has a job that builds and runs `WebIntegrationTests` with `--no-parallel`.\n+- [ ] The job maps each of the five secrets and sets `MULTITOOL_WEB_EXPECTED_PROVIDERS`.\n+- [ ] The workflow has a daily `schedule` trigger.\n+- [ ] The existing `CIWorkflowTests` still pass.\n+\n+## Tests\n+- [ ] Extend `Tests/FoundationModelsMultitoolTests/CIWorkflowTests.swift` with tests that pin: the `swift test --package-path WebIntegrationTests --no-parallel` line, the build line, each secret mapping, the `MULTITOOL_WEB_EXPECTED_PROVIDERS` line, and a `cron:` line (read the file that holds the job).\n+- [ ] Run `swift test --filter CIWorkflowTests`. All pass. Then run `swift test`.\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass. #web\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,30 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01M3A33HKP5H238CS992MAJYVS\n-- 01M3A33SMXZ93N45S1SWCZ6NMR\n-position_column: todo\n-position_ordinal: '8e80'\n-title: 'Web: add the web-integration CI job, the build step, and the daily schedule'\n----\n-## What\n-Run the web live package in CI. Design: `web.md` § \"Testing / CI\".\n-\n-- `.github/workflows/ci.yml`: keep the `ci` job (the call to the shared `swift-ci.yaml`) with no change. Add a job `web-integration` with `needs: ci`, on the same macOS runner class the shared workflow uses. Steps: checkout; `swift build --package-path WebIntegrationTests --build-tests`; `swift test --package-path WebIntegrationTests --no-parallel`. Map the repository secrets `BRAVE_SEARCH_API_KEY`, `TAVILY_API_KEY`, `EXA_API_KEY`, `SERPER_API_KEY`, `KAGI_API_KEY` to `env`, and set `MULTITOOL_WEB_EXPECTED_PROVIDERS` to the list of providers whose secrets the repository has.\n-- Add a `schedule:` trigger (daily, for example `cron: \"17 6 * * *\"`) so markup drift of the keyless providers shows before a user finds it. The `ci` job can keep running on the schedule too; if that is too expensive, put the web job in a new file `.github/workflows/web.yml` with the schedule and a `workflow_run` or `push` trigger, and pin that file instead.\n-- The build of `WebIntegrationTests` on every run keeps a broken live test visible, the same reason as `IntegrationTests/Package.swift:58-66`. If the shared workflow can take a second package path, prefer that input; else the build step in the new job is sufficient.\n-- Update the header comment of `ci.yml` to name the second package and why it reads secrets.\n-\n-## Acceptance Criteria\n-- [ ] The workflow file has a job that builds and runs `WebIntegrationTests` with `--no-parallel`.\n-- [ ] The job maps each of the five secrets and sets `MULTITOOL_WEB_EXPECTED_PROVIDERS`.\n-- [ ] The workflow has a daily `schedule` trigger.\n-- [ ] The existing `CIWorkflowTests` still pass.\n-\n-## Tests\n-- [ ] Extend `Tests/FoundationModelsMultitoolTests/CIWorkflowTests.swift` with tests that pin: the `swift test --package-path WebIntegrationTests --no-parallel` line, the build line, each secret mapping, the `MULTITOOL_WEB_EXPECTED_PROVIDERS` line, and a `cron:` line (read the file that holds the job).\n-- [ ] Run `swift test --filter CIWorkflowTests`. All pass. Then run `swift test`.\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass. #web\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M3A3GHQWCWDB0YW6727NZ0Z2","timestamp":"2026-09-24T16:20:53.116132Z","op":"update","item_id":"01M3A34A9941ZGD8FXMTHVF1SZ","forward_patch":"--- original\n+++ modified\n@@ -3,28 +3,29 @@\n - claude-code\n depends_on:\n - 01M3A33HKP5H238CS992MAJYVS\n+- 01M3A3FMG0S72N2Q515319Z6S4\n - 01M3A33SMXZ93N45S1SWCZ6NMR\n position_column: todo\n position_ordinal: '8e80'\n-title: 'Web: add the web-integration CI job, the build step, and the daily schedule'\n+title: 'Web: add the web.yml workflow (web-integration job, daily schedule)'\n ---\n ## What\n Run the web live package in CI. Design: `web.md` § \"Testing / CI\".\n\n-- `.github/workflows/ci.yml`: keep the `ci` job (the call to the shared `swift-ci.yaml`) with no change. Add a job `web-integration` with `needs: ci`, on the same macOS runner class the shared workflow uses. Steps: checkout; `swift build --package-path WebIntegrationTests --build-tests`; `swift test --package-path WebIntegrationTests --no-parallel`. Map the repository secrets `BRAVE_SEARCH_API_KEY`, `TAVILY_API_KEY`, `EXA_API_KEY`, `SERPER_API_KEY`, `KAGI_API_KEY` to `env`, and set `MULTITOOL_WEB_EXPECTED_PROVIDERS` to the list of providers whose secrets the repository has.\n-- Add a `schedule:` trigger (daily, for example `cron: \"17 6 * * *\"`) so markup drift of the keyless providers shows before a user finds it. The `ci` job can keep running on the schedule too; if that is too expensive, put the web job in a new file `.github/workflows/web.yml` with the schedule and a `workflow_run` or `push` trigger, and pin that file instead.\n-- The build of `WebIntegrationTests` on every run keeps a broken live test visible, the same reason as `IntegrationTests/Package.swift:58-66`. If the shared workflow can take a second package path, prefer that input; else the build step in the new job is sufficient.\n-- Update the header comment of `ci.yml` to name the second package and why it reads secrets.\n+- Create `.github/workflows/web.yml` with one job `web-integration`. Do not change `ci.yml`.\n+  - Triggers: `push` to `main`, `pull_request`, `workflow_dispatch`, and `schedule` with `cron: \"17 6 * * *\"` (daily). The daily run finds markup drift in the keyless providers. A separate file keeps the expensive real-model job of `ci.yml` off the daily schedule.\n+  - The job has no `needs`. It runs on the macOS 27 runner class that the shared `swift-ci.yaml` uses. Steps: checkout; `swift build --package-path WebIntegrationTests --build-tests`; `swift test --package-path WebIntegrationTests --no-parallel`.\n+  - The build step on each push and pull request gives the compile coupling that `IntegrationTests/Package.swift:58-66` asks for. The shared workflow has only one package-path input (`integration-package-path`), so the build step is in this job. `web.md` § \"CI\" records this change from the first design.\n+  - `env`: map the repository secrets `BRAVE_SEARCH_API_KEY`, `TAVILY_API_KEY`, `EXA_API_KEY`, `SERPER_API_KEY`, `KAGI_API_KEY`. Set `MULTITOOL_WEB_EXPECTED_PROVIDERS` to the provider names (not the secret names) of the secrets that `gh secret list` shows now, for example `braveAPI,tavily`. If no secret exists yet, set it to an empty string and write a comment that names the command to update it.\n+  - A header comment in STE says why the file is separate and why it reads secrets.\n\n ## Acceptance Criteria\n-- [ ] The workflow file has a job that builds and runs `WebIntegrationTests` with `--no-parallel`.\n-- [ ] The job maps each of the five secrets and sets `MULTITOOL_WEB_EXPECTED_PROVIDERS`.\n-- [ ] The workflow has a daily `schedule` trigger.\n-- [ ] The existing `CIWorkflowTests` still pass.\n+- [ ] `.github/workflows/web.yml` has the four triggers, the build line, the test line with `--no-parallel`, each of the five secret mappings, and the `MULTITOOL_WEB_EXPECTED_PROVIDERS` line.\n+- [ ] `ci.yml` has no change, and the existing `CIWorkflowTests` pass.\n\n ## Tests\n-- [ ] Extend `Tests/FoundationModelsMultitoolTests/CIWorkflowTests.swift` with tests that pin: the `swift test --package-path WebIntegrationTests --no-parallel` line, the build line, each secret mapping, the `MULTITOOL_WEB_EXPECTED_PROVIDERS` line, and a `cron:` line (read the file that holds the job).\n-- [ ] Run `swift test --filter CIWorkflowTests`. All pass. Then run `swift test`.\n+- [ ] Create `Tests/FoundationModelsMultitoolTests/WebWorkflowTests.swift`, modelled on `CIWorkflowTests.swift` (read the file with `RepositoryFile.read(relativePath: \".github/workflows/web.yml\")`). Pin: `cron:`, `pull_request`, the build line, the test line, each secret mapping, the `MULTITOOL_WEB_EXPECTED_PROVIDERS` line, and that the job has no `needs:` line.\n+- [ ] Run `swift test --filter \"WebWorkflowTests|CIWorkflowTests\"`. All pass. Then run `swift test`.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass. #web\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -3,29 +3,28 @@\n - claude-code\n depends_on:\n - 01M3A33HKP5H238CS992MAJYVS\n-- 01M3A3FMG0S72N2Q515319Z6S4\n - 01M3A33SMXZ93N45S1SWCZ6NMR\n position_column: todo\n position_ordinal: '8e80'\n-title: 'Web: add the web.yml workflow (web-integration job, daily schedule)'\n+title: 'Web: add the web-integration CI job, the build step, and the daily schedule'\n ---\n ## What\n Run the web live package in CI. Design: `web.md` § \"Testing / CI\".\n\n-- Create `.github/workflows/web.yml` with one job `web-integration`. Do not change `ci.yml`.\n-  - Triggers: `push` to `main`, `pull_request`, `workflow_dispatch`, and `schedule` with `cron: \"17 6 * * *\"` (daily). The daily run finds markup drift in the keyless providers. A separate file keeps the expensive real-model job of `ci.yml` off the daily schedule.\n-  - The job has no `needs`. It runs on the macOS 27 runner class that the shared `swift-ci.yaml` uses. Steps: checkout; `swift build --package-path WebIntegrationTests --build-tests`; `swift test --package-path WebIntegrationTests --no-parallel`.\n-  - The build step on each push and pull request gives the compile coupling that `IntegrationTests/Package.swift:58-66` asks for. The shared workflow has only one package-path input (`integration-package-path`), so the build step is in this job. `web.md` § \"CI\" records this change from the first design.\n-  - `env`: map the repository secrets `BRAVE_SEARCH_API_KEY`, `TAVILY_API_KEY`, `EXA_API_KEY`, `SERPER_API_KEY`, `KAGI_API_KEY`. Set `MULTITOOL_WEB_EXPECTED_PROVIDERS` to the provider names (not the secret names) of the secrets that `gh secret list` shows now, for example `braveAPI,tavily`. If no secret exists yet, set it to an empty string and write a comment that names the command to update it.\n-  - A header comment in STE says why the file is separate and why it reads secrets.\n+- `.github/workflows/ci.yml`: keep the `ci` job (the call to the shared `swift-ci.yaml`) with no change. Add a job `web-integration` with `needs: ci`, on the same macOS runner class the shared workflow uses. Steps: checkout; `swift build --package-path WebIntegrationTests --build-tests`; `swift test --package-path WebIntegrationTests --no-parallel`. Map the repository secrets `BRAVE_SEARCH_API_KEY`, `TAVILY_API_KEY`, `EXA_API_KEY`, `SERPER_API_KEY`, `KAGI_API_KEY` to `env`, and set `MULTITOOL_WEB_EXPECTED_PROVIDERS` to the list of providers whose secrets the repository has.\n+- Add a `schedule:` trigger (daily, for example `cron: \"17 6 * * *\"`) so markup drift of the keyless providers shows before a user finds it. The `ci` job can keep running on the schedule too; if that is too expensive, put the web job in a new file `.github/workflows/web.yml` with the schedule and a `workflow_run` or `push` trigger, and pin that file instead.\n+- The build of `WebIntegrationTests` on every run keeps a broken live test visible, the same reason as `IntegrationTests/Package.swift:58-66`. If the shared workflow can take a second package path, prefer that input; else the build step in the new job is sufficient.\n+- Update the header comment of `ci.yml` to name the second package and why it reads secrets.\n\n ## Acceptance Criteria\n-- [ ] `.github/workflows/web.yml` has the four triggers, the build line, the test line with `--no-parallel`, each of the five secret mappings, and the `MULTITOOL_WEB_EXPECTED_PROVIDERS` line.\n-- [ ] `ci.yml` has no change, and the existing `CIWorkflowTests` pass.\n+- [ ] The workflow file has a job that builds and runs `WebIntegrationTests` with `--no-parallel`.\n+- [ ] The job maps each of the five secrets and sets `MULTITOOL_WEB_EXPECTED_PROVIDERS`.\n+- [ ] The workflow has a daily `schedule` trigger.\n+- [ ] The existing `CIWorkflowTests` still pass.\n\n ## Tests\n-- [ ] Create `Tests/FoundationModelsMultitoolTests/WebWorkflowTests.swift`, modelled on `CIWorkflowTests.swift` (read the file with `RepositoryFile.read(relativePath: \".github/workflows/web.yml\")`). Pin: `cron:`, `pull_request`, the build line, the test line, each secret mapping, the `MULTITOOL_WEB_EXPECTED_PROVIDERS` line, and that the job has no `needs:` line.\n-- [ ] Run `swift test --filter \"WebWorkflowTests|CIWorkflowTests\"`. All pass. Then run `swift test`.\n+- [ ] Extend `Tests/FoundationModelsMultitoolTests/CIWorkflowTests.swift` with tests that pin: the `swift test --package-path WebIntegrationTests --no-parallel` line, the build line, each secret mapping, the `MULTITOOL_WEB_EXPECTED_PROVIDERS` line, and a `cron:` line (read the file that holds the job).\n+- [ ] Run `swift test --filter CIWorkflowTests`. All pass. Then run `swift test`.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass. #web\n\\ No newline at end of file\n","transaction_id":null}
