---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m11kemht5n37nnh4227gnz7x
  text: |-
    Research and implementation notes:
    - The sibling source has no `swift-log` `Logger`. `SchemaConverter` reports each drop through the `onDrop` closure (`SchemaConversionLogHandler`). The port keeps that closure and adds no logger.
    - The three types are now internal. The explicit `public init` of `SchemaIR.Property`, `SchemaConversionLogRecord` and `SchemaConversion` was identical to the synthesized memberwise initializer, so it is deleted.
    - The repo holds no formatter config file (no `.swift-format`, no `.swiftlint.yml`). No formatter was run.
    - The fixtures stand in `Tests/FoundationModelsMultitoolTests/MCPFixtures/` and `Package.swift` declares `.copy("MCPFixtures")`. Both converter suites load them through `Bundle.module` with one shared loader, `loadMCPSchemaFixture(_:)`, in `Tests/FoundationModelsMultitoolTests/Fixtures/MCPSchemaFixtures.swift`. The sibling duplicated that loader in two files.
    - The test `LogRecorder` uses `Mutex` from `Synchronization`, as `TestScratch` does, in place of the sibling's `NSLock` + `@unchecked Sendable`.
    - Numeric literals in the ported tests are named `static let` constants, and each `case let .x(a, b)` pattern is written as `case .x(let a, let b)`.
    - `MCPCapability.swift` header now states that the three codec files are present.
  timestamp: 2026-08-27T12:34:54.138951+00:00
- actor: claude-code
  id: 01m11keptfk8hw61hqz86eqn6e
  text: |-
    ### implement — changed
    - evidence: 10 files — Sources/FoundationModelsMultitool/Capabilities/MCP/SchemaConverter.swift, GeneratedContentCodec.swift, Value+ScalarString.swift, MCPCapability.swift; Package.swift; Tests/FoundationModelsMultitoolTests/SchemaConverterGuideTests.swift, SchemaConverterStructureTests.swift, GeneratedContentCodecTests.swift, Fixtures/MCPSchemaFixtures.swift, MCPFixtures/ (13 json). `swift build --build-tests` clean; `swift test --filter SchemaConverter` 24 passed; `swift test --filter GeneratedContentCodecTests` 16 passed.
    - next: test, commit, review
  timestamp: 2026-08-27T12:34:56.463577+00:00
depends_on:
- 01M11286356T1VMGHBSMF9V7W5
position_column: doing
position_ordinal: '80'
title: Port SchemaConverter and GeneratedContentCodec into Capabilities/MCP
---
## What
Port the codec pair that eventplan.md § "Consolidation of the siblings" names: "the `SchemaConverter` / `GeneratedContentCodec` pair". This is a behavioral port: keep the logic the same, and write new header comments in the `//` block style of `Capabilities/Shell/ShellCapability.swift`.

- Source: `/Users/wballard/github/swissarmyhammer/FoundationModelsMCP/Sources/FoundationModelsMCP/SchemaConverter.swift` (759 lines), `GeneratedContentCodec.swift`, and `Value+ScalarString.swift`.
- Target: `Sources/FoundationModelsMultitool/Capabilities/MCP/SchemaConverter.swift`, `GeneratedContentCodec.swift`, `Value+ScalarString.swift`.
- `SchemaIR`, `SchemaConversion`, `SchemaConversionLogRecord`, and `GeneratedContentCodecError` stay as they are. Replace the `swift-log` `Logger` with `os.Logger`, or with the `SchemaConversionLogHandler` closure that the source already has.
- Keep the types internal, the way the Shell capability does. Tests reach them with `@testable import`.
- The converter feeds `MCPTool` (a later task): a JSON Schema from `tools/list` becomes a `GenerationSchema` for the verb's arguments, and the codec turns the model's `GeneratedContent` into `[String: MCP.Value]` for `tools/call`.

## Acceptance Criteria
- [ ] Each JSON Schema shape the source converts gives the same `SchemaIR` here (structure, guides, `$ref` definitions, unsupported keywords logged and dropped).
- [ ] `GeneratedContentCodec.arguments(from:)` gives the same `[String: Value]` for the same `GeneratedContent`.
- [ ] `swift build` succeeds.

## Tests
- [ ] Port `SchemaConverterGuideTests.swift`, `SchemaConverterStructureTests.swift`, and `CodecTests.swift` from `/Users/wballard/github/swissarmyhammer/FoundationModelsMCP/Tests/FoundationModelsMCPTests/` into `Tests/FoundationModelsMultitoolTests/`. Name the ported codec suite `GeneratedContentCodecTests` (file and `@Suite`), so the filter below matches. Port the fixtures they read from `Tests/FoundationModelsMCPTests/Fixtures/` into `Tests/FoundationModelsMultitoolTests/MCPFixtures/` and declare that folder as a `.copy` resource in `Package.swift`, the way `FilesGoldens` is declared.
- [ ] `swift test --filter SchemaConverter` passes.
- [ ] `swift test --filter GeneratedContentCodecTests` passes.

## Workflow
- Use `/tdd` — port the tests first, then port the code to make them pass. #eventplan #phase-4