---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m3ev1bgjv6kzw3dxpwt5ek5z
  text: |-
    ### implement — research
    - The production code exists. The three suites test it live. They need no production change.
    - `Fetch(context:).call(arguments:)` gives the fetch result. `WebContext(configuration:sessionConfiguration:)` uses the real `SystemHostResolver`, thus the guard tests go through real DNS.
    - `WebPageReader.networkLoadCount` is actor state: read it with `await context.reader.networkLoadCount`.
    - Exact guard texts (from `WebAddressGuard`): `The address is not allowed: localtest.me resolves to 127.0.0.1, a loopback address.` and `The address is not allowed: the host 169.254.169.254 is on the blocklist.` The second is a blocklist refusal before DNS, thus it sends no request.
    - Exact binary text (from `WebFetcher`): `The content type is not text: application/pdf. fetch reads text, HTML, JSON, and XML.`
    - `FetchResult.contentType` holds the media type with no parameters, thus `text/plain` compares exactly.
    - The unit fixtures `RunOutput` and `WebRun` are in the root test target. The `WebIntegrationTests` package cannot import them.
    - DuckDuckGo challenge decision: `.keyless` tries `braveHTML` first. A DuckDuckGo challenge can reach the runCode test only after Brave fails. Then Brave's failure is the real failure. The test does not assert which keyless provider answered, and does not read `notes`. Thus a DuckDuckGo challenge alone cannot fail it.
  timestamp: 2026-09-26T12:28:58.770892+00:00
- actor: claude-code
  id: 01m3ev672zb1xvt7chag8p9682
  text: |-
    ### implement — changed
    - evidence: 5 files — WebIntegrationTests/Tests/FoundationModelsMultitoolWebIntegrationTests/FetchLiveTests.swift (new, 5 tests), GuardLiveTests.swift (new, 2 tests), WebRunCodeLiveTests.swift (new, 1 test), Support/LiveFetch.swift (new, shared context and fetch call), Support/LiveSearch.swift (`makeShortTimeoutConfiguration()` is now internal, not private, for reuse; doc text). `swift test --package-path WebIntegrationTests --no-parallel`: 13 tests in 6 suites passed, 2 known issues (DuckDuckGo challenge in `DuckDuckGoHTMLLiveTests`). Root `swift test`: 1790 tests in 143 suites passed. The only warning is the vendored mlx-swift "missing creator for mutated node" build warning, outside this repository.
    - note: DuckDuckGo served its challenge page during this run, and `WebRunCodeLiveTests` still passed, because Brave answered first. No production change was necessary. The tests test code that exists, thus no RED step was possible. Each exact expected text comes from the production source.
    - note: the description checkboxes are checked. The tag `web` stays.
    - next: /review.
  timestamp: 2026-09-26T12:31:38.079222+00:00
- actor: claude-code
  id: 01m3evpb1qzdcf347h6st4c9k4
  text: |-
    ### test — green
    - evidence: `swift test` — 1790 tests, 143 suites, 0 fail, 0 skip. `swift build --build-tests --package-path IntegrationTests` — build OK, 0 fail. `swift test --package-path WebIntegrationTests --no-parallel` — 13 tests, 6 suites, 0 fail, 2 known issues (DuckDuckGo challenge page, per the 2026-09-26 decision, not a fail).
    - warning: one build-system warning in all three runs: "missing creator for mutated node" for `mlx-swift_Cmlx.bundle/Contents/MacOS`. This comes from the vendored mlx-swift dependency build step. It is not code in this repository. It does not make this outcome red.
    - next: none. The step is done.
  timestamp: 2026-09-26T12:40:26.423004+00:00
depends_on:
- 01M3A33HKP5H238CS992MAJYVS
position_column: doing
position_ordinal: '80'
title: 'Web: add live fetch, guard, and runCode tests to WebIntegrationTests'
---
## What
Add the live suites that fetch real pages and run the goal snippet, with no model. Design: `web.md` § "Testing / Level 2", rows `FetchLiveTests`, `GuardLiveTests`, `WebRunCodeLiveTests`.

Create under `WebIntegrationTests/Tests/FoundationModelsMultitoolWebIntegrationTests/`, each `.serialized` with `.timeLimit(.minutes(1))`:
- `FetchLiveTests.swift` (`@testable import FoundationModelsMultitool`):
  - `https://example.com`: title `Example Domain`, content contains `Example Domain`.
  - `http://github.com`: final URL starts with `https://github.com`.
  - `https://en.wikipedia.org/wiki/Swift_(programming_language)` with `maxCharacters: 2000`: `nextOffset` is set; a second window through the same `WebContext` gives the next text, and `WebPageReader.networkLoadCount` stays at 1.
  - `https://api.github.com/zen`: `contentType` is `text/plain`, content is not empty.
  - `https://www.w3.org/WAI/ER/tests/xhtml/testfiles/resources/pdf/dummy.pdf`: the binary correction.
- `GuardLiveTests.swift`: `http://localtest.me/` (a public DNS name that resolves to `127.0.0.1`): the correction names the loopback address; `http://169.254.169.254/latest/meta-data/`: a correction.
- `WebRunCodeLiveTests.swift`: a real `MultiTool` with `.withWeb(configuration: .keyless)` runs the snippet of `web.md` § "Goal" and returns 1 to 3 pages, each with a title and content.

## Acceptance Criteria
- [x] Each case above passes on a machine with network access.
- [x] No test retries.

## Tests
- [x] The three files above.
- [x] Run `swift test --package-path WebIntegrationTests --no-parallel`. All pass.

## Workflow
- Use `/tdd` — write failing tests first, then implement to make them pass. #web